# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

灵动岛桌面挂件(Windows):Electron 透明无边框悬浮窗,复刻 iOS 灵动岛,通过 Windows SMTC 监听 QQ音乐/网易云等播放状态并控制(切歌/进度/模式),内置本地播放器兜底。技术栈:Electron 43 + React 19 + TypeScript + Vite,全部中文注释。

## 常用命令

```bash
pnpm dev             # 仅 Vite Web 演示版(浏览器调试,不进 Electron)
pnpm dev:widget      # 构建挂件页面并启动 Electron(日常调试主入口;**已前置 pnpm build:electron**——dev:widget 自动重建 agent.cjs/bridge.cjs,改 electron/agent/*.ts 不再静默跑旧 bundle(2026-08-06 修复,曾实测新工具在对话里调不到、LLM 拿旧工具凑合);必须带 --mode widget,
                     # 否则 tracks.ts 的内置三首 MP3 会打进挂件歌单,而音频文件又被
                     # closeBundle 删掉 → 播放列表出现"无法播放且无法删除"的损坏曲目)
pnpm build           # tsc -b 类型检查 + Web 版构建
pnpm build:widget    # 仅构建挂件页面 → dist-widget/
pnpm build:electron  # esbuild 打包 SMTC 桥接(→ electron/bridge.cjs)+ Agent 引擎(→ electron/agent.cjs)+ 生成图标
pnpm dist:win        # 完整打包(electron-builder:便携版 + NSIS 安装版 → release/)
pnpm lint            # oxlint
pnpm bridge          # 独立运行系统媒体桥接脚本(单独调试 SMTC)
pnpm watch:electron # 热重建 Agent 引擎/桥(监听 electron/agent/*.ts 与 scripts/,自动 esbuild 重建 + 重启 electron;渲染端仍用 dev:widget)
node scripts/test-agent-core.mjs   # Agent 引擎核心测试(后端直测,不经 UI;
                     # esbuild 打包测试 bundle + electron 别名 stub + 真实
                     # mock MCP stdio/sse 服务器,78 个用例)
pnpm test:markdown    # 消息气泡 Markdown 解析器测试(esbuild 打包纯解析器
                     # 直测,39 个断言:块级/行内/流式退化/递归不循环)
```

**验证约定(用户要求,2026-08-04 更新;2026-08-07 再更新)**:每次优化/修改代码后,自动重新构建并启动实机验证——默认执行 `pnpm dev:widget`(构建挂件页面并启动 Electron)确认改动生效,此前的"默认不自动验证"约定已废止。实机验证用 `timeout` 包住 electron 在几十秒后自动退出(应用托盘常驻不会自退,见下方调试工具),需要截图时配 `WIDGET_SCREENSHOT=<path>`;**巡检约定(2026-08-07 用户要求):重新构建程序后不要自动跑 WIDGET_SCREENSHOT 巡检**——完整巡检(agent 模式等)只在用户明确要求时执行(每轮全量巡检耗时 8-10 分钟且依赖真实 LLM,频繁自动跑不划算);默认完成标准 = 构建 + dev:widget 启动 + 类型检查 + lint(`tsc -b` / `pnpm lint`)+ 单测(`node scripts/test-agent-core.mjs`)。例外:`pnpm dist:win` 打包出安装包仍只在用户明确要求时执行。

## 架构

### 双入口共享一个组件

- `src/` — Web 演示版入口(vite.config.ts),带完整演示页面。
- `widget/` — 桌面挂件入口(vite.widget.config.ts,`mode=widget`),只渲染灵动岛本体;`base='./'` 使产物可直接 file:// 加载。该构建模式下 `src/media/tracks.ts` 返回空内置歌单、产物剔除 music 资源。
- **`src/components/DynamicIsland/DynamicIsland.tsx` 是两端共享的岛体组件**,行为差异全部靠 CSS 覆盖(widget/widget.css 的 `.widget-stage` 选择器)与可选 props 区分。改动组件时务必同时考虑 Web 版(App.tsx)与挂件版(WidgetApp.tsx)两个调用方。
- `tsconfig.app.json` include 含 `src` + `widget`;`pnpm build` 的 `tsc -b` 会检查两端。

### 数据源双轨

`WidgetApp` / `App` 中 `useSystemMedia`(SMTC 外部平台)与 `useMediaPlayer`(本地播放器)并存,`externalActive = system.active && system.track && useExternalSource` 决定数据与控制走哪一边。切换数据源时**双向暂停**(切走的一方自动暂停,避免双声齐响)。进度/时长/播放模式/曲目都按此分支取数。SMTC 播放模式以系统真实状态为数据源(轮询校准),点击循环后 1.2s 检测是否跟随,没跟随则提示并回退。

### Electron 主进程(electron/main.cjs)

- 透明无边框窗口 + `setIgnoreMouseEvents` 点击穿透:`widget-stage`(岛体)mouseenter/leave 经 IPC `widget:pointer` 切换"接收鼠标"。
- **移动挂件 = 右键长按拖拽**(WidgetApp 的 stage 层):按住右键 ~0.4s(位移 < 8px)进入拖拽模式后拖动,快速右键点击/拖动无效果。渲染端指针捕获 + IPC(`widget:drag-start/move/end`)→ 主进程用"窗口 = 鼠标 - 按下偏移"绝对定位,位置自由(不限制在屏幕内)。防漂移三件套:① 拖拽激活基准用长按期间的最新坐标(不是按下点,消除手抖偏移);② 渲染端 <0.5px 容差去重(窗口移动合成的亚像素事件不发送);③ 主进程 setPosition 后校验实际落点,不一致就重算偏移(自校正,不累积相对偏移)。**拖拽合帧与自校正降频**(2026-08-06:渲染端 pointermove rAF 合帧——每帧至多一次 IPC,松手先发末帧再 drag-end;主进程自校正 getPosition 降频 ~10Hz,绝对定位不累积误差,每事件一次同步 OS 往返纯浪费);**窗口 IPC 统一 isDestroyed 防护 + 主进程 uncaughtException/unhandledRejection 兜底**(2026-08-06:退出/销毁竞态下在途 IPC 调窗口方法会抛原生异常(实测 drag-move setPosition "conversion failure"),防默认错误弹窗退进程);set-size/set-height 主进程 100ms trailing 合帧(流式长高每秒多次 OS 窗口 resize → 压到 ~10 次/秒,保留最后一次);**展开位置补偿(2026-08-08 修复"首次展开偏右"与"展开向右位移"双 bug)**:`win.setSize` 锚点 = 左上角,变宽时窗口右扩 → 岛体视觉右偏(启动顶部居中的窗口展开最明显,实测需手动拖回)——set-size handler 在 setSize 后按 `dx = (旧宽-新宽)/2` 补偿 `setPosition`,**宽度变化保持窗口中心 X 不动(岛体左右对称展开),高度变化保持顶部不动(向下生长)**;补偿在 setSize 之后、下一帧渲染前完成,视觉无跳变。**只按请求宽度变化补偿(二轮修复)**:Windows 透明无边框窗口实际宽比请求宽大 ~2px(实测 1242 vs 1240),若每次 set-size(仅高度变化,Agent 面板高度渐进每帧上报)都按 (ow-cw)/2 补偿,宽度偏差被当成真实变化,每帧右移 1-9px,展开动画期间累积 80px+(实测 wx 从 237 漂到 318 = "展开向右位移")——`lastSetSizeW` 记忆上次请求宽,`|cw - lastSetSizeW| >= 2` 才做 X 补偿(高度渐进帧位置零漂移,最小脚本实测:展开/收起中心偏移 ≤1px,高度 20 帧漂移 0);render-process-gone crashed/oom 自动 reload(防崩溃白屏必须手动重启)。**异常防线**:所有拖拽坐标过 ±10 万合理范围校验(真实屏幕坐标不可能超出),非有限值或超界一律丢弃并打日志 — `setPosition` 的 int32 参数转换对超界有限值(|v| ≥ 2^31)会抛未捕获异常,绝不能把脏数据传进去。拖拽期间穿透保持接收(鼠标移出岛体也持续响应),松手按指针位置恢复穿透。进度条/文字手势均加了 `button !== 0` 守卫,右键只属于拖拽。
- SMTC 桥接以 `utilityProcess` 启动 `electron/bridge.cjs`(由 `scripts/system-media-bridge.ts` 经 esbuild 打包),崩溃自动重启(10s 内 3 次上限);桥内部 smtc-reader 子进程同款上限(10s 内 3 次放弃,含 spawn error——脚本缺失时 spawn 只发 error 不发 exit,不处理会桥活着但 SMTC 永久死,2026-08-06)。
- 配置持久化 `userData/settings.json`(仅 alwaysOnTop);主题色 localStorage(`widget-theme-color`);上传音乐 IndexedDB(`island-uploads`);自定义背景图 IndexedDB(`island-background`)+ 背景参数(不透明度/裁切)localStorage(`widget-background`,旧单独不透明度键 `widget-background-opacity` 仅迁移读取);自定义字体文件 IndexedDB(`island-font`,data URL,10MB 上限,ttf/otf/woff/woff2)+ 参数 localStorage(`widget-font`)。窗口位置每次启动顶部居中,不持久化。**卸载可选保留/删除数据**(2026-08-06,用户要求):`deleteAppDataOnUninstall: false`(原 true 确认卸载即删、无勾选框),改由 `nsis-custom.nsi` 的卸载页面接管——`customUnWelcomePage` 钩子(assistedInstaller.nsh 检测到已定义就用它替代默认卸载欢迎页)在标准欢迎页**之前**插入 nsDialogs 复选框页「保留我的数据(推荐)」**默认勾选**(防误删,数据删除不可恢复);`customUnInstall` 里 `$IslandKeepData != BST_CHECKED` 才 `RMDir /r "$APPDATA\dynamic-island"`(打包后 userData 目录名 = package.json 的 `name`,不是中文 productName);静默卸载(/S)跳过页面按保留处理。**nsis-custom.nsi 必须带 UTF-8 BOM**:NSIS 的 `!include` 只按 BOM 检测编码(`-INPUTCHARSET` 只作用于主脚本),无 BOM 的 UTF-8 中文在 GBK 系统报 "Bad text encoding"(实测;上次"安装版"从未成功打包,release 里只有便携版)。页面/函数都写在宏体内:该文件被 !include 在 installer.nsi **最前面**(MUI2/nsDialogs 未加载),宏到 assistedInstaller.nsh 的插入点才展开。**开始菜单卸载入口**:electron-builder 26 辅助安装不生成独立的"卸载"快捷方式,由 `nsis.include: electron/nsis-custom.nsi` 补齐(customInstall 创建「卸载 灵动岛挂件.lnk」指向 `$INSTDIR\Uninstall 灵动岛挂件.exe`,customUnInstall 卸载时删除;宏体内 `${UNINSTALL_FILENAME}`/`${SHORTCUT_NAME}` 调用时才展开,安全)。
- **单击展开误修复已撤销(2026-08-10 用户澄清)**:挂件版展开 = **长按**(450ms),单击留给文字区快捷手势(双击播放/暂停、三连击切 Agent、左滑右滑切歌)——此前把"清除数据后无法交互"误判为"单击不展开"并加了 onChange 缺失时 changeExpanded(true),用户实测文字区快捷手势全部失效,恢复原逻辑(handleClick 非展开态:onChange 缺失 return)。"清除数据后无法交互"根因 = **穿透死锁(2026-08-10 二轮定位)**:收起时岛体/窗口收缩,鼠标滑出岛体 → mouseleave → 穿透开启;穿透态下 OS 不再投递鼠标事件(forward 的 mousemove 转发在 Windows 不可靠),移回岛体时 mouseenter 永不触发 = 悬浮/点击无响应。修复三层:① **穿透轮询校正**(main.cjs `widget:pointer-poll` invoke + preload pointerPoll + WidgetApp 动态轮询:读窗口 bounds + 光标屏幕位置,与岛体 rect(viewport + bounds)核对,状态不一致即 pointer(inside) 校正——完全绕开事件可靠性;拖拽期间跳过;正常 mouseenter/leave 仍走事件,幂等;**挂载立即校正一次 + 穿透开启时 200ms 高频、穿透关闭时 600ms 低频**(2026-08-10 修复"清除数据后悬浮要等更久才生效":清除数据 reload 后窗口从设置大面板收缩回 520×280(顶部不动),光标停在原面板底部被甩出窗口 → mouseleave → 穿透开;移回岛体收不到事件,恢复延迟受轮询周期限制 = 用户感知的"悬浮时间变长"——穿透开启是事件被吞的危险状态,高频轮询把恢复压到 ≤200ms 不可感知,穿透关闭时事件流正常,600ms 低频省 IPC;挂载首 tick 另修复 lastPointerPollRef 为 null 时(回退比较得 actual=false)光标恰在岛体上的首周期延迟));② **切接收补发 mousemove**(main.cjs widget:pointer active=true 时,光标在窗口内则按当前位置 sendInputEvent mouseMove——轮询校正了穿透但页面无新 mousemove,Chromium 不重算 hover,mouseenter 不触发,实测移回后 hover 仍 false;补发后立即重算恢复);③ 单击展开撤销(见上——长按才是展开,单击留给文字区手势)。**三轮修正(2026-08-10 用户实测"清除后长按呼出展开态无法交互")**:穿透轮询的 last 记忆与事件竞态——mouseleave(展开动画/hover 重算瞬间误触发)开穿透后,轮询 inside 仍 true、last 仍 true → 永不校正 = 穿透保持开(forward 只转发 move 不转发 down/up → hover 正常但点击全丢)。修复:handleStageEnter/Leave/RootMouseLeave **同步更新 lastPointerPollRef**(事件与轮询共用意图状态);轮询改以主进程**实际穿透状态**为准校正(pointer-poll 返回 `win.isIgnoreMouseEvents()`——43 实测该方法不存在,typeof 容错回退"意图比较");isIgnoreMouseEvents 不可用时轮询仍按 last 校正。**巡检真实鼠标教训**:SetCursorPos 只移动光标不按下——click 计数 0 是假阳性,真实点击必须 user32 `mouse_event`(0x0002 down / 0x0004 up);展开态 PANEL-INTERACT 断言 = hover + 真实点击 down/up 计数(穿透拦截时到不了页面)。**真实 OS 鼠标验证(SetCursorPos)**:sendInputEvent 直接注入页面绕过穿透是假阴性(PASS 误报);巡检"收起后交互"段用 PowerShell Cursor::Position 移动真实鼠标:边缘长按收起(鼠标滑出岛体)→ 移出窗口 → 移回岛体 → 断言 hover 恢复(轮询 + 补发 mousemove 双保险);INTERACT-AFTER-CLEAR 断言单击/长按/Agent 模式全展开(单击不展开是设计,real-mouse 段必须长按)。待用户补充具体现象(猜测:清除后 mode 回 music、API Key 被删需重填、或 Agent 设置面板内清除后 reload 的特定状态)。**新增 WIDGET_SCREENSHOT_MODE=clear-data 巡检**:完整清除链路(localStorage.clear + 删 5 库 + agentClearData('app') 删 userData 文件)→ 800ms 后 reload → 断言:页面/岛体/设置桥/IndexedDB 可重建/模式 + **真实鼠标(sendInputEvent)单击/长按/Agent 模式展开**(合成 PointerEvent 不经命中测试是假阳性——此前合成 600ms 长按"能展开"掩盖了单击失效);用户数据(userData 文件 + localStorage)备份恢复 + resetSettingsCache。巡检坑:settingsPath 是函数需调用;主进程上下文无 document;交互前 ensureCompact(展开态长按=收起,顺序耦合);断言须真实鼠标(sendInputEvent),合成 PointerEvent 假阳性。
- 托盘"设置…"经 IPC `widget:open-settings`(preload 的 `onOpenSettings` 订阅)通知渲染端,`DynamicIsland` 的 `requestSettingsSeq` prop(seq 递增触发)展开面板并切换到**设置视图**。背景编辑器 / 主题色 / 字体均从设置视图内部进入(控制区不再有主题色按钮;Web 演示版入口 = 控制区 `settingsButton`)。**帮助手册已整体移除(2026-08-10 用户要求)**:HelpView/SettingsView 入口/preload onOpenHelp/main.cjs 首启引导/WidgetApp HELP_WIN_W 与补间 animateWindowTo/layout PanelView 'help'/help 相关 CSS 全部删除;设置视图保留 背景/主题色/字体/Agent 设置/歌词 API 五入口;首启仅落盘 firstRun 标记。此前(2026-08-06~08-10)曾有:大尺寸教学版(岛体 800×640,JS 宽度特判 + .island-help-view)、初次安装自动打开、设置↔帮助窗口补间动画(animateWindowTo,多次修复抖动后最终按用户要求直接移除)。**设置类视图**(`settings`/`background`/`theme`/`font`/`font-color`/`font-library`/`image-library`,组件内 `SETTINGS_VIEWS` 常量)一律屏蔽单击岛体、长按、Esc、点击面板外等一切缩回操作,只能通过返回键退出(子视图返回回设置视图,设置返回收起)。**蒙版为岛体层持久层 `.island-panel-mask`**(`expanded && panelView !== 'control'` 时挂载,z-index 0 在岛体背景之上、面板之下,`rgba(8,10,14,0.55)` 不透明度恒为 1):面板自身不再带蒙版背景,否则面板的 `maskPanelIn` 淡入动画会连蒙版一起透明,视图切换瞬间背景图透出闪烁。蒙版 0.55 为调低后的值(原 0.78 背景图几乎全黑、控件发黑)。**蒙版下控件统一"中性胶囊"设计**(参考背景编辑器"更换图片"等按钮,`island-bg-actions .island-ctl`):返回键 `island-bg-back` 与上传键 `island-ctl--upload` 不用强调色(transparent 混色叠在蒙版上成"黑洞",实底混色又太黑),统一为半透明白底 `rgba(255,255,255,0.09)` + 细白描边 `rgba(255,255,255,0.24)` + 白字,悬停加深(0.12/0.32)。面板进入动画统一纯淡入(`maskPanelIn`),不做位移回弹(蒙版随 translateX 回弹会像整块甩动)。四个子视图返回键 UI 统一(扁平 `island-bg-back`,占满整行、强调色描边)。
- **字体库与图片库(font / font-library / image-library 视图)**:字体与图片都支持多条目库(IndexedDB 按 id 存条目,`island-font` store 'fonts'、`island-background` store 'library',旧单键数据自动迁移;参数 localStorage `widget-font` 存 currentFontId/colorMode/colorValue)。库页面为**大面板**(`island-lib-view`,岛体 440px,宿主窗口同步 480,与背景编辑器共用 TALL 高度机制):搜索框按名称过滤、列表/网格点击应用、行内编辑名称(Enter 提交/Esc 取消/失焦提交)、删除(删当前应用字体则回退默认)、上传(自动入库并应用;同一 dataUrl 不重复添加)。**图片库网格行高必须显式 `grid-auto-rows: 128px`**:auto 行高 + `align-content: start` 时,内容一旦超高(卡片实高约 125px)Chromium 会把行压缩到恰好填满容器((高度-间距)/行数,实测 17 张 → 26px/行),卡片压扁、名称被 overflow:hidden 裁掉、内容永不过高也无法滚动;字体列表 `.island-lib-row` 同理需 `flex-shrink: 0` 防 flex 压缩。背景上传/选择经宿主 `handleBackgroundChange` **自动入库**(命名"背景图 N"),字体上传在组件内查重后 `onFontAdd`。**图片库读取逐条容错(2026-08-08 修复"图片库永远空/初次进入无法右键")**:library store 一条损坏记录(blob 文件丢失,Chromium 报 NotReadableError "Data lost due to missing file",实测)会让 getAll 整个事务失败 → loadImageItems 恒返回 [] → 图片 tab 永远空。改为 getAllKeys 取键 + **逐条独立事务 get**——坏记录只失败自身、其余正常,失败条目尝试删除(数据不可恢复,移除毒瘤自愈);loadImageItems 失败不再静默(console.error 暴露真实错误)。巡检模式 `WIDGET_SCREENSHOT_MODE=media-lib`(多媒体库 UI 巡检:面板/试听自动播放/编辑动画/宽度对齐/右键菜单应用背景/视频 autoPlay,注入走桥正规路径 + 背景备份恢复,79 用例)。上传的字体注入 @font-face(组件内动态 style 标签 `island-font-face`,字体族 `island-font-custom`,岛体 inline font-family 覆盖、fallback 取运行时 body 字体栈;按钮/输入 font-family: inherit 跟随)。颜色模式 `auto` = **合成亮度算法**:背景图以 opacity 叠加在岛体深底 rgb(8,10,14) 上,可读性取决于合成后亮度(像素 × opacity + 深底 × (1-opacity)),取**当前形态**背景图(展开态用展开图、紧凑态用紧凑图,缺图退另一形态),32×32 采样平均 >140 判亮 → 黑字 `#0b0b0f`,否则白字;`custom` = **独立颜色页**(`font-color` 视图,字体视图"自定义"入口):色板 + **岛内自绘取色器** + hex 输入(防抖 200ms)。取色器为可复用组件 `IslandColorPicker`(SV 面:横向饱和/纵向明度,色相条横向渐变,pointer capture 拖动取色,`hsvToHex` 实时提交;**不弹系统 `<input type=color>` 对话框**,UI 不出岛;**字体颜色与主题色共用**);拖拽期间 `pickingRef` 屏蔽 prop 回读(避免 8bit 量化往返换算导致光标抖动),非拖拽时从当前色同步归位。**SV 面明度黑渐变必须拆成独立 `::after` 层**(两层渐变叠在同一 background 时黑渐变会在底边提前淡出,底部露出一条亮色带,像素实测;独立层 + inset:0 铺满,底部实黑)。**取色面/色相条不带边框**:1px 边框 + `inset:0` 的 ::after 只覆盖到 padding box,边框内侧会露出一圈底色渐变(底部为亮色环,像素实测),去掉边框后 ::after 全覆盖、无线条。颜色页(字体 `font-color` / 主题色 `theme` 视图共用 `island-color-main` 布局)岛体 352px(挂件窗口 364,宿主 `VIEW_WINDOW_H` 登记;SV 面 flex:1 吃满剩余高度);字体视图本身固定 200px 紧凑高度。文字颜色经 CSS 变量注入岛体:`--text-color`(主文字,原 `#fff` 全部替换为 `var(--text-color, #fff)`)、`--text-dim`(次级文字,原白色系 rgba 透明度 0.3~0.75 全部替换为 `var(--text-dim, …)`,值为主色 55% 透明度);无设置时变量缺省 → CSS fallback 白色系,外观不变。背景状态 = `{expandedImage, compactImage, opacity: {expanded, compact}, expanded: {zoom,posX,posY}, compact: {zoom,posX,posY}}` — **展开态与紧凑态各有独立的图片、不透明度与裁切**(图片 IndexedDB 双槽位 `expanded`/`compact`,旧版单图自动迁移到两个槽位;opacity 旧版单一数值自动迁移为双槽位同值;参数 localStorage `widget-background`,旧版单形态参数迁移到展开态;背景参数读写抽在 backgroundStore(`readBackgroundParams`/`saveBackgroundParams`,2026-08-06:WidgetApp/设置桥/Web 演示版三处共用,旧数据迁移行为一致——此前桥只认双槽位,旧格式下 LLM 工具读到 0.4 而 UI 显示真实值;openIdb 打开失败不再缓存失败 Promise,瞬时错误后功能可自愈))。背景图层经 CSS 变量按形态切换(`--bg-img-e/--bg-size-e/--bg-pos-e`、`--bg-img-c/--bg-size-c/--bg-pos-c`),不透明度在组件内按 `expanded` 取对应槽位,参考比例:展开 400×244、紧凑 280×56;编辑器分段切换编辑目标(上传/移除/不透明度滑杆均作用于当前形态)。视口切形态时圆角**瞬切**(不做圆角动画,避免"矩形裁切到圆角"观感),高度与取景弹簧过渡。背景编辑器蒙版由岛体层 `.island-panel-mask` 统一提供(编辑控件压在自定义背景图上保证可读性),裁切视口(预览框)自身不透明显示原图、不受蒙版影响。背景视图需要更高空间:岛体加 `island-bg-view` 类(440px),切紧凑态编辑时折叠为 `island-bg-view--compact`(288px,折叠掉多余岛体,高度弹簧过渡),宿主经 `onPanelViewChange` → IPC `widget:set-height` 调整窗口高度(480 ↔ 280),离开视图回落。**背景图必须经 `downscaleBackgroundImage` 降采样(长边 ≤1024px)**:岛体形变逐帧重栅格化大图是带背景切换卡顿的主因,上传与旧图加载都走该函数。
- 调试工具:`WIDGET_SCREENSHOT=<path>` 加载后自动截图;`WIDGET_SCREENSHOT_MODE` 支持 `expanded` / `layout`(输出面板各区域几何 JSON,验证布局用)/ `theme` / `stress` / `test` / **`agent`**(Agent 功能严格 UI 巡检:托盘设置 → Agent 设置视图 → 表单字段/四个区(MCP 服务/技能目录/长期记忆/自我进化)逐项断言 → MCP 类型切换 + 真实连接 mock 服务器(env `WIDGET_MOCK_SERVER` 指向 mock-mcp-stdio.cjs)→ 记忆增删 → **记忆类型按钮本体滚轮切换断言**(合成 `new WheelEvent('wheel')` 即可驱动 React onWheel——deltaY 符号与 DOM 一致,正向 = 下一项;正向/反向各一格断言 + `.tick` 类 + 下拉保留(点击展开 4 选项 → 点外关闭))→ 进化触发 → 保存 → 返回收起;两段式:段 1 进视图截图(`.agent1.png`),段 2 交互断言 JSON;段 4.5 **快捷切换按钮用 `sendInputEvent` 注入真实鼠标**(合成 MouseEvent 不触发 CSS :hover):悬浮 ⋯ → 浮现断言 + 截图(`.agent-quick.png`)→ 横移过间隙中点不断开 → 双向滚轮逐格断言(期望序列由 DOM 推断的 menuItems 计算;**sendInputEvent 的 deltaY 符号与 DOM wheel 事件相反,实测 +120 → 反向一步**,正向注入 -120)→ 单击跳转断言;段 4 末停在对话历史视图,段 4.5 开头先返回聊天视图——也修复了段 5 自动回复在错误视图轮询的既有问题;**段 4.7 灵动岛设置工具端到端**(2026-08-06:直接调主进程 runIslandSettings 绕开 LLM 保证确定性——主题色 → 断言 `--state-color` 即时生效;缩放 150 → 断言岛宽按比例变化(localStorage 读当前缩放,比例容差 10px);背景导入(1×1 PNG data URL)→ 断言 `--bg-img-e` 生效 + 图库条目;改名 → 断言;字体导入 → 断言库条目 + widget-font 含新 id;改名 → 断言;不存在的 id 改名 → 断言拒绝。**前后状态备份恢复**:localStorage 设置项(主题色/缩放/背景参数/字体参数)+ 背景双槽位 IndexedDB 原图(原生事务 get/put,put null=清空),测试条目按「巡检测试」名前缀删除,恢复后手动补发 island-settings-changed 事件让 UI 重读——不残留用户数据)。`WIDGET_SCREENSHOT_QUIT=1`:截图/巡检完成后优雅退出(app.quit)——**必须带**:应用托盘常驻不自退,测试命令若用 timeout/taskkill 强杀进程树,子进程(bridge/GPU/renderer)被杀会打出 `renderer gone: crashed` 假象(实测误导)。跑实机验证时配合 `timeout` 启动 electron 并在几十秒后自动退出。

### SMTC 桥接

`scripts/system-media-bridge.ts` → `electron/bridge.cjs`;读取端为 `electron/smtc-reader.ps1`,运行时用 `csc.exe` 编译 `electron/smtc-bridge.cs`(Windows 11 26100 新版 API,引用 System32\WinMetadata),异步等待用轮询 Status/GetResults(3s 超时防卡死)。PS 5.1 无法绑定 WinRT 集合元素,所以必须走 C# 强类型桥接。

### 架构优化(2026-08-06,三代理审计落地)
- **electron 侧 TS 纳入编译检查**:tsconfig.electron.json 并入 tsc -b(此前 esbuild 打包不查类型,electron/agent/*.ts 全部类型错误静默;修复了一批真实错误:MemoryStoreLike 缺 importEntries、evolution getLog 返回 Promise、tools.ts 的 deps 悬空引用/exec 选项/fs.existsSync 错用、deepseek usage 缺 cached_tokens、AgentTool.execute 类型;AgentConfig.excludedTools/excludedSkills 改可选)。
- **settings.json 持久化加固**(main.cjs):内存缓存(loadSettings 不再每次同步读盘,agent 每步 4-8 次)→ 0;原子写(tmp + rename + .bak 损坏恢复)+ 150ms 防抖 + before-quit flush——原直接写目标文件,强杀截断后下次保存以 {} 为基底覆盖写回整份丢失;apiKey 经 safeStorage(DPAPI)加密落盘(enc: 前缀,解密失败回退 null 重填)。
- **dev 链路**:agent.cjs/bridge.cjs 不再入库(.gitignore + git rm --cached);dev:widget 前置 build:electron;main.cjs require 前存在性 guard;新增 pnpm watch:electron 热重建脚本。
- **双宿主共享 hook**(src/hooks/useIslandCustomizations.ts + useIslandMedia.ts):App.tsx 与 WidgetApp.tsx 原 ~540 行逐字重复(主题色/提示/背景图+图片库/字体库/媒体数据源派生)收敛为共享实现;对齐两端分叉(lyrics platformId、useCallback 稳定性、cycleMode 1.2s 回退经 ref 读最新值);LLM 设置工具的事件重读收进各 hook(WidgetApp 的 onSettingsChange effect 删除)。
- **设置变更事件 typed 包装**(settingsBridge.onSettingsChange/emitSettingsChange):事件名/scope/注销统一,消字面量漂移(原 DynamicIsland 裸字符串 + 三处强转)。
- **main.cjs 测试代码抽离**:WIDGET_SCREENSHOT 六种巡检模式 ~1160 行 → electron/screenshot-tests.cjs(deps 注入,main.cjs 2242 → 1090 行)。
- **Electron 安全显式化**:webPreferences 显式 sandbox/webSecurity;挂件页 CSP meta(script/style/img/font/media/connect 白名单,file:// 实测渲染正常;`media-src 'self' blob:` 必须保留——缺它时上传音乐的 blob URL 音频被 CSP 拦截无法播放,2026-08-08 实测);setWindowOpenHandler deny。
- **AgentView 消息组件群搬移**:UserBubble/ToolCard/ToolSummary/AssistantBlock → views/AgentMessages.tsx(AgentView 1416 → 1204 行)。
- **总结标题长度统一**:引擎 sanitizeTitle 与渲染端兜底均为 10 码元(原 10 vs 12);IslandSettingsOp 补 delete 两 op。
- **有意未做**(审计结论):手势/展开状态机抽 hook(350 行,历史 bug 密集无测试,收益与风险不成比例)、main.cjs 全量 CJS 拆分(先抽测试块,文件继续膨胀再拆)、~~exec_command 首轮确认门~~(已落地 2026-08-06:`agent.confirmExec` 开关,设置视图可改,默认关;开启后每轮首个命令经 tool-confirm-request 事件 → AgentView 确认卡[允许/拒绝] → agent:tool-confirm 回执,120s 超时拒绝,拒绝结构化回填 LLM 可自纠;引擎 `createTurnConfirmGate` 工厂(测试导出,61 用例含确认门)、CSS 单文件拆分(行为中性,可维护性收益,留待抽组件时顺手搬)、preload/main @ts-check(全量 JSDoc 成本高,以共享常量+类型化包装替代)、DynamicIsland 再抽 useAgentScale/useScrubControl 与面板视图注册表(代码注释已完善,拆分收益低于风险,待下次交互改造时顺手)。

### Agent 模式(2026-08-05 新增,不动音乐播放器代码)

托盘右键菜单"模式"在**音乐模式 ↔ Agent 模式**间切换(radio,持久化 `settings.json` 的 `mode`,启动恢复;渲染端 `onSetMode` 订阅 + `getMode` 兜底)。**模式切换事件带来源**(`widget:set-mode` payload `{mode, source}`):`source: 'tool'` = Agent 工具 switch_to_music 触发——属于对话流程,切回音乐时**不中止**正在运行的本轮(引擎完成回复并落定消息;若中止,最终回复被丢弃,历史停在未答复的用户消息,下一轮 LLM 把旧请求当"仍待执行"重复执行 = 上下文污染,实测"打开B站"时又被自动切回音乐模式);`'user'` = 托盘/手势,切回音乐时中止当前轮(语义不变)。Agent 模式 = 桌面挂件里的 LLM 对话 + 本机工具执行(无沙箱),架构参照 opencode 源码(packages/opencode/src 的 message parts 模型 / LLM 流式循环 / 工具注册语义)与 MS Agent 参考后端(单 agent ReAct 循环,砍掉 multi-agent pipeline)。

- **引擎**:`electron/agent/*.ts`(types/deepseek/anthropic/chat/provider/tools/mcp/skills/memory/evolution/engine)→ esbuild 打包 `electron/agent.cjs`(零第三方依赖,`electron` external;入口仍是 engine.ts,evolution 经 re-export 打进同一产物),主进程内运行(非 utilityProcess:纯异步网络/文件 IO)。**无状态**:渲染端每次 send 回传完整历史(参考后端"客户端持有历史"模式)。**Provider 按 Base URL 自动判定**(`detectProvider` 在 provider.ts,engine 与 evolution 共用:地址含 "anthropic" → Anthropic Messages;含 "chat" → DeepSeek Chat Completions(备选);否则(**默认**)→ DeepSeek Responses API):
  - **DeepSeek Responses API(默认 provider)**:streamResponse,裸 fetch + SSE,POST {baseURL}/responses,默认 https://api.deepseek.com,模型 deepseek-v4-flash(官方指南 https://api-docs.deepseek.com/zh-cn/guides/responses_api + API 参考 https://api-docs.deepseek.com/zh-cn/api/create-response)。**顶层参数**:model / input(字符串或 item 列表)/ instructions / stream / max_output_tokens(**含思维链 token**)/ tools(function / web_search 服务端执行)/ tool_choice(auto 默认)/ reasoning / text.format / temperature·top_p(思考模式下不生效)/ user(标识用户,可 KVCache 隔离);不支持的参数静默忽略、无状态 API。**reasoning.effort 官方值域**:none / minimal / low / medium / high / xhigh / max——none = 关闭思考模式(设置页思考强度新增"关");minimal·low = 低强度,medium·high = 高强度,max = 最高;不传用模型默认(开启)。**text.format**:text(默认)/ json_object(JSON 模式,prompt 需含 "json" 字样)/ json_schema(结构化输出,name + schema 必填)——总结标题走 json_object。**输入 items**:message(角色 user/assistant/system/developer 视同 system;content 仅 input_text/output_text,**reasoning 内容块 400** "unknown variant" 实测)、function_call(call_id 必填唯一,每个 call 必须有对应 function_call_output)、function_call_output(output 字段)、reasoning(明文 content 归并相邻 assistant 消息;**必须回传**,缺失 400 "The reasoning_text in the thinking mode must be passed back")、web_search_call(原样回传自动恢复搜索结果)。**usage**:input_tokens_details.cached_tokens(上下文缓存命中)/ output_tokens_details.reasoning_tokens(思维链 token 数)。**工具参数流式累积修复(2026-08-08,用户实测 write_file 参数一直为空)**:实测 DeepSeek 的 `response.function_call_arguments.delta` **不带 call_id**(带 output_index),权威完整参数在 **`response.function_call_arguments.done`**(带 item_id)——原实现 delta 按 call_id 匹配全部丢弃、done 事件未处理,参数只能靠 `output_item.done` 恰好携带 item.arguments 兜底,响应被截断时参数恒为空 → 工具失败 → LLM 重试又截断 = 空参调用死循环。修复:`findCall` 按 call_id → output_index → item_id 三级匹配累积增量;处理 function_call_arguments.done 作权威参数;output_item.added 记录 itemId/outputIndex;`response.incomplete` 打日志(reason 多为 max_output_tokens)便于排查。**备选 Chat Completions provider**(chat.ts,地址含 "chat" 自动切换):官方 multi_round_chat / tool_calls / json_mode / strict 指南体系,支持 v4-flash 与 v4-pro,response_format json_object(实测含工具历史约 60% 空白 content——官方"有概率返回空 content",Responses 的 json_object 实测空返回率远低)。
  - **Anthropic Messages API**(`streamAnthropic`,`POST {baseURL}/v1/messages`,x-api-key 鉴权(官方文档:anthropic-version 被忽略,保留头兼容原生);适用于 DeepSeek Anthropic 兼容端点 `https://api.deepseek.com/anthropic` 或原生 `https://api.anthropic.com`;max_tokens 必填 4096;模型名 claude 前缀由 DeepSeek 自动映射到 v4 系列,未知名自动映射 deepseek-v4-flash)。**格式已对照官方兼容性表核对**:tools name/input_schema/description ✓、tool_choice 未发(默认 auto)、消息 content 仅 text/tool_use(id/input/name)/tool_result(tool_use_id/content)✓、thinking 内容块/thinking_delta 流支持(转发为 reasoning-delta 事件,UI 深度思考)。与 Responses 的格式差异:角色严格交替(相邻同角色合并);**工具结果必须打包进下一条 user 消息的 tool_result 块**(parts 模型里 tool-call/tool-result 成对,序列化时重排);同一条助手消息里 tool_use 之后不能再有文本;工具参数是流式 JSON delta(input_json_delta.partial_json);reasoning parts 回放时丢弃(thinking 块需要 signature,两端兼容性取舍)。两个 provider 同构返回 `ProviderOutcome {calls, text, usage, aborted}`,引擎循环共用。
  - 工具结果截断 8000 字符回填上下文(两 provider 一致)。
  - **thinking 模式 reasoning 必须回传**(400 "The reasoning_text in the thinking mode must be passed back"):引擎每轮(含工具循环)把思维链 reasoning part 存入助手消息 parts,历史序列化按 provider 输出——Responses(默认)= 独立 reasoning item;Chat = assistant 消息 `reasoning_content` 字段;Anthropic 路径 thinking 块需 signature 不可回放,已丢弃——工具调用后的下一轮请求必须携带上一轮思维链,只在最终消息里存会丢。
- **工具系统(模块化)**:`{name, description, parameters(JSON Schema), execute}` 注入 LLM 上下文(LLM 据此生成参数,过程全程可知——执行前 tool-call 事件展示完整参数,结果 tool-result 事件回显)。v1 工具:exec_command(无沙箱)/ read_file / write_file / list_dir / open_url / open_file(**媒体文件拦截 = media 附件(2026-08-08 用户要求"说打开视频看看,结果打开的是外部播放器;LLM 回复已播放但窗口看不到视频气泡")**:图片/视频/音频扩展名命中时不调 shell.openPath 弹外部播放器,返回 `{ text, media: [{kind, url, name}] }`——引擎注入助手消息 **media part**(AgentPart 新增 `{type:'media', kind, url}`),渲染端 AssistantBlock 渲染 MediaFrame **窗口内直接播放**(不依赖 LLM 输出 markdown,实测 LLM 只回"已播放"不展示;AgentTool.execute 返回类型扩展 media 附件,与 image 附件同机制);exec_command 描述同样注明勿用 start 打开媒体;**exec_command 也拦截 start 打开媒体**(2026-08-08 二轮修复"LLM 总说已开始播放但窗口看不到气泡":LLM 播放视频常用 `start "标题" "路径"` 而非 open_file——extractMediaPathFromStart 解析 start 命令(双引号形式取第二段、裸 token 取首词,相对路径按 cwd 解析,单引号串=纯标题不拦截)返回媒体附件,mediaKindForPath 与 open_file 共用;测试 87 用例含两段引擎端到端(mock LLM 调 open_file / exec_command start → message parts 含 media))/ web_search(HTML 端点免 Key)/ get_time / system_info / notify / switch_to_music(切回音乐模式)/ **bili**(B站数据查询与视频下载,调内置 bili-tool(2026-08-07 随包发行改造:三个外部工具移植进项目 `tools/`,**仅 bili 打进安装包**(extraResources → resources/tools/bili;xxt/DocFlow 不随包,打包版缺失时工具报「工具缺失」,dev 回退源码 + 系统 Python),引擎 `toolsRoot()` 按 `process.resourcesPath` / cwd 双环境解析;spawn 注入 **`BILI_BASE_DIR = userData/bili`**(bili-tool base_dir 支持该环境变量——exe 在安装目录只读,cookies.json/登录态落 userData);**bili-tool 登录态热搜修复(2026-08-07)**:登录过期时 nav 接口无 wbi_img → 空 mixin → `orig.as_bytes()[i]` 越界 panic(热搜/全部 wbi 请求崩溃)——wbi_keys 提取失败**降级游客 nav 重试**(游客 nav 必返回 wbi_img),仍失败返回明确错误;sign_wbi 加长度防御;源码 = Desktop/bilibili/bili-rs(修复后 exe 已拷 tools/bili,src/Cargo.toml/Cargo.lock 同步,可独立重编译);**对话内扫码登录(2026-08-07,用户方案:LLM 消息气泡展示二维码图片)**——bili-tool login 支持 `--qrcode-img <path>`(qrcode + image crate 生成 PNG,scale 4 = 132px,PNG ~4.4KB → base64 ~5.9KB 在引擎 8000 截断内)与 `--no-wait`(仅生成不轮询;CLI 层 no_wait 生成即成功退出,不报「登录未完成」);引擎 bili 工具新增 **login action**(生成二维码 → 读 PNG 转 data URL → 返回文本+图片附件,LLM 原样展示给用户扫码;2026-08-07 修复:原只生成二维码,扫码后无人 poll 写登录态,whoami 永远未登录——bili-tool login 加 `--resume <key>`(跳过 generate 直接轮询)+ no-wait 输出 `二维码key:`;引擎解析 key 后 `startBiliLoginPoll` 后台 spawn `login --resume --timeout 120`(注册到**通用后台任务注册表**,见下),成功(退出码 0,写 cookies.json)发通知 + **background-done 自动触发对话告知登录成功**,失败/超时**同样进入终态触发对话反馈**(LLM 在对话里告知"二维码过期可重新生成",不再只弹系统通知))与 **whoami action**(登录状态确认);**B 站扫码登录两个关键坑(实测)**:① poll 响应**顶层 code 恒为 0,真正状态在 data.code**(86101 等待/86090 已扫码/86038 失效/0 成功)——误读顶层 code 把未扫码当登录成功 → url 空 →「缺少 SESSDATA」;② 成功响应的 url 是 **crossDomain?ticket= 中转地址,SESSDATA 在请求该地址的 Set-Cookie 里**(302 到 www.bilibili.com 时下发)——CLI login 原只解析 url query(serve.rs 已走 fetch_cookies_chain,CLI 漏接);修复:login.rs poll 读 data.code + SESSDATA 缺失时走 `fetch_cookies_chain`(reqwest Jar 不做域检查);**消息气泡 data URL 图片渲染**(markdownParser:data:image/ → `img` 节点 → `AgentImage` 组件,远程 http(s) 仍走链接策略;CSP img-src 已含 data:);**按比例展示(2026-08-07 用户要求,配合界面缩放)**:`AgentImage`(Markdown.tsx 导出,消息气泡 img 与 markdown 图片共用)——显示宽度 = 原图宽(naturalWidth)× 缩放系数 `--agent-s`(岛体根变量,此前 CSS 注释声称 JS 设置但从未落盘,已补:agentActive 时 = agentScale/100)**× 1/4**(用户实测仍太大,缩小为四分之一:132px 二维码 100% 缩放显示 33px),面板缩放时图片等比放大,超宽图 CSS max-width: 100% 兜底压缩保持比例;**标题长度放宽(2026-08-07 用户要求)**:总结标题从 ≤8 汉字改为**推荐 10 字左右、严格不超过 20 字**——系统提示三级措辞、TITLE_LITERAL_EXAMPLES 示例词、sanitizeTitle 硬截断 10→20 码元、渲染端 label runner 兜底截断参数化(标题 20 / 心理揣测 16,原统一 16 会把放宽后的标题截到 16);紧凑态文字区随字数扩展岛宽(20 字约 420px < MAX_WIDTH_PX);**下载 cwd 固定 BILI_CWD = userData/bili**(安装目录只读,落点改 `userData/bili/downloads/`——config 的 outdir=downloads 相对此解析;不固定 cwd 会落在 Electron 启动目录(打包版可能是 System32/exe 目录),用户和 LLM 都找不到,实测;**BILI_CWD 目录必须存在(2026-08-08 修复)**:模块加载时 `mkdirSync(BILI_CWD, {recursive:true})`——spawn 的 cwd 不存在会 ENOENT 且被误报"二进制缺失",download/saved/trending 等分支直接 spawn 从不建目录,首次使用必炸,实测;mkdirSync 必须从 node:fs 顶层导入(promises 命名空间没有它,曾调 undefined 被空 catch 吞掉,静默失败);**绝对路径约定**:启动返回文本带输出目录、完成通知与后台任务状态注入(`getBiliBackgroundStatus`)列输出文件绝对路径(close 后 `bili saved --limit 10` 解析落点目录下的记录)、`bili saved` action 把记录里的相对路径逐行转绝对——LLM 据此能准确告知"视频在哪个文件夹";`--outdir` 相对参数同样按 BILI_CWD 解析)/ **doc_convert**(文档转换,对接内置 DocFlow 服务 http://127.0.0.1:5000:**首次调用自动拉起服务**(2026-08-07 随包发行:打包 = 内置 docflow.exe(pyinstaller 全量打包,重依赖 PyMuPDF/pdf2docx/opencv/scipy/rapidocr 内置;server.py 已内置 sys.frozen 分支,BASE_DIR = exe 目录,资源相对放置),dev = 系统 python + `tools/docflow/server.py`;轮询 /api/engine 就绪,60s 超时,并发互斥单例防多次拉起;服务常驻,`disposeTools` 随引擎 dispose 清理)→ multipart 上传(files+mode=to_markdown)→ /api/convert → 轮询 status → 下载到输出目录)/ **get_feature_guide**(2026-08-10 用户要求"加一个工具让 LLM 读技术文档,告诉用户有什么功能、进行引导":读取 `docs/TECH.md`(约 3000 行技术文档,第 11 章 = 功能清单与使用引导知识库),按 topic 关键词返回相关章节(纯函数 `extractGuideSections` 按 `##` 二级标题切分、标题或正文开头 400 字含关键词即命中、每节截 2500/最多 5 节/总长截 8000 防撑爆上下文,测试导出;无 topic 返回标题树);路径 `guideDocPath()` 双环境解析:打包版 extraResources → resources/docs/TECH.md(electron-builder.yml 已收录),dev = 项目根 docs/;描述引导 LLM"用户问有什么功能/怎么用 XX 时先调用,挑 3-5 个重点介绍,不要一口气列完";工具描述与文档 11.18 引导话术配套。README.md(用户向)/ WIDGET-README.md(挂件部署调试)/ docs/TECH.md(技术文档)三件套 2026-08-10 重写,改功能后按 TECH.md 附录 D 清单同步)/ **xxt**(超星学习通自动答题,打包 = 内置 xxt.exe(pyinstaller onedir,playwright 内置,浏览器走系统 Edge channel),dev = 系统 python + `tools/xxt/auto_answer.py`;**登录态/截图目录经环境变量 XXT_PROFILE_DIR/XXT_SCREENSHOT_DIR 隔离到 userData/xxt-profile**(原 .browser_profile 含用户登录态,绝不随安装包分发);子命令 login/crawl/fill(--answers JSON)/check/submit/screenshot,--url 必填,login 300s 超时其余 180s)/ **灵动岛设置工具**(2026-08-06,`electron/agent/settingsTools.ts`,LLM 对话中直接改挂件设置**即时生效**:set_theme_color(hex 校验归一化)/ set_agent_scale(100-300 钳制)/ import_font(ttf/otf/woff/woff2 ≤30MB,读盘转 data URL → 入库并应用为当前字体)/ list_fonts / rename_font / import_background(png/jpg/gif/webp/bmp ≤20MB → 降采样 → **展开+紧凑双槽位都应用**+入库)/ list_library_images / rename_library_image / **set_font_color**(自定义 hex 或 mode=auto 恢复按背景亮度自动黑白)/ **set_background_opacity**(展开态/紧凑态槽位单独或一起改,0-1 钳制,只改提供的槽位、另一个保持现值;桥读现有参数只改 opacity 写回)/ **get_island_settings**(2026-08-06,设置快照读取:主题色/缩放/文字颜色/背景不透明度/当前字体——**修改前先查当前值**,setter 桥返回值都带 previous 原值,工具回复自动成「从 X 调整为 Y」,已是目标值则跳过写并提示「无需修改」;修复 LLM 不知原有缩放把 300% 当默认值;2026-08-10 快照补媒体窗口默认宽)/ **set_media_window_size**(160-800 钳制,对话媒体窗口默认宽)/ **多媒体库 9 工具**(2026-08-10 起可用):list/import/rename/remove_audio_library(import 读盘校验 → data URL ≤200MB 经桥解码 ArrayBuffer)+ list/import/rename/remove_video_library(import 校验扩展名/存在/≤10GB 记路径)+ **play_library_video**(2026-08-10,跳转多媒体库视频 tab 并**立即播放指定视频**:桥校验条目存在后派发 `island:media-library-play` 事件 → WidgetApp 展开面板 + 记 pending id → DynamicIsland 传 MediaLibraryView `autoPlayVideoId` → 切视频 tab/清搜索后展开该行自动播放,消费后清回 null 防重复触发;与 open_file 区分:open_file = 对话内媒体附件,本工具 = 多媒体库面板播放)。**白名单漏加修复(2026-08-10,实测 import_audio_library 等全部不可用)**:main.cjs `ISLAND_SETTINGS_OPS` 只有首批 13 个 op,媒体窗口/多媒体库 10 个 op 工具层与桥都注册了但白名单漏加 → 调桥报「未知的设置操作」——补 setMediaWindowSize + 音频库 4 + 视频库 4 + playLibraryVideo;工具清单 23 个(测试断言同步;2026-08-10 加 set_video_config + list_conversation_media)。**list_conversation_media(2026-08-10 用户要求"LLM 接入当前对话窗口有什么作为附件列出的多媒体元素,知道哪个在播放,视频的音量/循环/速度/全屏")**:桥 op getConversationMedia 遍历消息气泡 DOM(.island-media-frame 的视频/图片 + .island-agent-voice 音频;视频岛/多媒体库播放器不在容器内不列入——语义 = 对话窗口附件)——视频带 playing/volume(0-100)/speed/loop/fullscreen/position/duration,音频带 playing;MediaFrame 容器加 `data-media-name`(alt 来源,桥读取名字);工具格式化输出(视频含「正在播放/音量 X%/速度 Xx/循环开/非全屏/进度」),空清单兜底;配合 set_video_config 调整(先查当前值再改)。**set_system_volume(2026-08-10 用户要求"支持调整系统音量",tools.ts 内置工具)**:winmm waveOutGetVolume/SetVolume 读/写系统主音量(0-100;低 16 位左 + 高 16 位右声道)——Windows 无内置音量 CLI,CoreAudio COM 接口方案本机 E_NOINTERFACE 实测失败(GetDefaultAudioEndpoint out 参数 QI 失败),waveOut 映射默认渲染设备零 COM 最稳;脚本 electron/system-volume.ps1(打包 extraResources → resources/bridge/,与 smtc-reader.ps1 同款);action=get 查询/set 设置;测试只测错误路径(成功路径真实改系统音量,不测)。**set_video_config(2026-08-10 用户要求"调整对话窗口内正在观看视频的设置"+ "灵动岛单独声音")**:volume = 灵动岛独立音量(0-1,只影响岛内媒体,与系统音量互不影响——系统音量用 set_system_volume)/ speed(0.5-2)/ loop / fullscreen(true 进入全屏,false 退出)/ width(160-800 媒体窗口默认宽),参数全可选至少一个;桥三 op:getVideoPrefs(音量/速度/循环/媒体窗口宽/全屏状态)/ setVideoPrefs(写 videoPrefs 模块 → island:video-prefs 事件 → 三处播放器订阅实时同步,已挂载播放器即时生效)/ setFullscreen(对话窗口内**正在播放**的视频容器 requestFullscreen,优先非 paused 否则最后挂载;false exitFullscreen);get_island_settings 快照补视频行(音量/速度/循环/全屏)。工具清单 21 个(测试断言同步)。**架构**:存储全在渲染端(localStorage/IndexedDB),工具经 `EngineDeps.runIslandSettings`(main.cjs 注入)→ executeJavaScript 在页面上下文调 **`window.__islandSettings` 设置桥**(`src/settingsBridge.ts`,仅挂件版 WidgetApp 注册;与设置界面 UI 同款存储层:backgroundStore/fontStore)→ 桥写存储后派发 `island-settings-changed` 事件(detail.scopes)→ **WidgetApp 监听重读**(主题色 setCustomTheme / 背景图与参数 / 字体参数与库 / 图片库)、**DynamicIsland 监听重读 scale**(agentScale 状态)→ 即时生效,无需进设置界面。桥的 deleteFontItem/deleteLibraryImage 不暴露给 LLM(防误删),巡检清理用。工具未注入桥时不注册(Web 演示版)。**EngineDeps 注入**:`runIslandSettings?(op, args)`。文件读取/校验在工具层(扩展名/存在性/大小/hex/名称长度),桥错误统一 {error} 抛给引擎按"工具执行失败"回填。引擎循环:流式 → 有 function_call 则逐个执行(60s 兜底超时,失败结构化回填供 LLM 自纠)→ 回填历史续调,**工具参数校验(2026-08-08,LLM 自主纠错核心机制)**:`validateRequiredArgs`(engine.ts 导出,测试覆盖)按工具 schema 的 required 校验,缺参/空串/解析失败(_raw)生成**结构化错误文本回填**(列出缺失参数名 + 类型 + 说明 + enum 可选值 + 解析失败原文),**不执行工具**——LLM 看到"缺什么、怎么补"自行修正重试,不再反复空参调用(用户实测 write_file 空参死循环的兜底防线);校验在 executeToolBatch(主循环/子代理)与手动调用路径都生效;数值 0 / 布尔 false 是合法值不误判,无 required 的工具跳过,**迭代上限 25** 防死循环(原 10 轮实测太紧:复杂任务 LLM 试错空间不足频繁撞上限,用户反馈"试错成本太低"→ 放宽;上下文增长由 trimHistory 预算治理兜底)。**静默总结标题**:**独立的总结后台 Sub Agent**(`createSummaryAgent`,主进程独立单例,与主对话引擎**零共享**——独立实例/AbortController/每次调用独立读配置,主对话的发送/中止/清空/模式切换都打不断总结,总结失败也不外溢到对话);无工具单轮(系统提示"对话标题生成器,**≤8 个汉字**"——紧凑态文字区约 6-9 字,长标题被岛体截成"开头几字"观感等同总结失败,实测),事件不转发 UI,**强制 low effort 加速**;**45s 超时**失败返回空串(工具结果多 + 思考模式 + 高峰期,15s 实测太紧;无工具请求不携带 tools/tool_choice,请求体最小化);**输入压缩**:最近 12 条消息,reasoning 截 500 字、工具结果截 2000 字(标题只需主题,大请求是超时的隐藏原因);**总结走 json_mode 三级降级链**(默认 Responses 路径 = text.format json_object,实测空返回率远低;Chat 路径 = response_format json_object,实测含工具历史时约 60% 空白 content——官方"有概率返回空 content"的严重形态):① JSON 措辞 A → ② JSON 措辞 B(官方建议"修改 prompt 缓解")→ ③ 纯文本兜底(不设 format,历史上可靠);每级 noThinking(官方 effort 'none' 关闭思考——标题无需思考,且输出 token 全花在思维链上是空白 content 的典型场景),parseTitleJson 解析逐级回退;**JSON 格式修复(2026-08-05)**:① prompt 示例值会被模型照抄当标题(实测"不超过8个汉字的简短标题"被原样输出)——措辞改为只描述结构("{"title": "<对话标题>"}")并明确禁止照抄示例,`TITLE_LITERAL_EXAMPLES` 命中示例词视为无效进入下一级;② parseTitleJson 健壮化——依次尝试 原文 / 剥离 ```json``` 代码块 / 从第一个 { 截取 / **第一个 { 到最后一个 } 截取(容忍尾随内容)**,任一解析出合法 title 即返回(测试:代码块包裹/前缀文本/尾随内容/非法回退);**严格解析(2026-08-06,修复标题变 "['data']")**:JSON 措辞改用 `extractJsonTitle` **严格解析**——必须解析出合法 JSON 对象的字符串 title 才采信,解析失败返回空串进入下一级(原 parseTitleJson 解析失败会**把原文整串兜底当标题**,模型在 json 模式输出 Python 列表字面量 "['data']" 时就被当了标题,实测);额外容忍 **Python 风格单引号 dict**(`{'title': 'xxx'}`,模型 json 模式常输出;解析失败后把单引号替换为双引号再试一次,值内含双引号时无法归一化 → 拒绝,安全侧);纯文本兜底措辞才允许原文回退;所有措辞统一拒绝**代码字面量**标题(`looksLikeCodeLiteral`:`[...]`/`(...)` 括号包裹,如 ['data']);**每级独立容错**(2026-08-05:每个 attempt 最多尝试 2 次,调用失败(超时/网络)重试一次后进入下一级——旧实现调用失败直接跳出循环、整个总结放弃,是"经常没总结"的结构性原因);**90s 超时**(45s 实测高峰仍超);**输入压缩补 tool-call 参数**(compressArgs 递归截断字符串值 200 字——大参数如 write_file 内容/exec_command 长命令是隐藏的大请求源,此前只压 reasoning/工具结果);**sanitizeTitle 清洗截断**:去首尾引号/书名号(兜底)+ 剥「标题:」「对话标题是」前缀与尾随句末标点(纯文本措辞下模型常输出"标题:xxx"/"xxx。",不剥会得到"标题:怎样配置M"这类错标题)+ 按 code point 截 10 码元,渲染端再兜底 12 码元;`agent:summarize` IPC 走 `getSummaryAgent()`。**确定性兜底(2026-08-07)**:全部尝试失败(空 content/调用失败/垃圾输出)返回 `fallbackTitle(messages)`——取**首条用户消息**文本截 10 码元作标题,保证标题永不为空(LLM 偶尔全挂时空标题 = 文字区永远显示回复开头)。**心理揣测 Sub Agent(2026-08-07)**:`createMindAgent`(engine.ts,与总结/主对话引擎均零共享,`agent:mind-guess` IPC + preload `agentMindGuess` + main.cjs `getMindAgent` 懒加载单例)——根据当前对话**揣测 LLM 回复时的心态**,≤16 汉字俏皮话(2026-08-07 放宽:15 字左右最多 16,紧凑态文字区随字数扩展岛宽)、推荐 10 字左右(如「表面淡定,内心在慌」),系统提示禁照抄示例(`MIND_LITERAL_EXAMPLES` 命中判无效)、`sanitizeMind` 剥「心理揣测:」前缀/尾随标点、**超长自查重生成**(2026-08-07 用户要求:生成结果超 16 码元 = 文字区截断残片(实测"知道了喵～人家刚才在装乖,其实已"每次都截断)→ **重新组织直到不截断**(重试上限 MIND_MAX_RETRIES=5 次防死循环,全部失败返回空串由调用方回退——不显示截断残片;空/垃圾/照抄示例同样重试);**措辞强化一轮过**(2026-08-07:系统提示明确"必须严格控制在 16 个汉字以内 + 输出前先数一遍字数,超过就删减"——原"最多不超过"措辞模型常不遵守,是反复截断的结构性原因)、**紧凑态宽度自适应**(2026-08-07:DynamicIsland 紧凑态 agent 模式**跳过像素截断**——文字已按码元截断且无进度条,岛宽随文字长度扩展(16 字约 340px < MAX_WIDTH_PX 500),渲染端兜底 truncateCodepoints 16)、60s 超时单措辞 + 一次重试(增强显示,失败由渲染端回退标题/回复预览),输入压缩与总结共用 `recentMessages`。**接入记忆与提示词系统(2026-08-07)**:系统提示与主引擎**同源**——main.cjs 注入 `getMemoryStore`/`getEvolution`(与主引擎同款单例),`buildMindSystem` 拼装「自定义提示词 + 长期记忆块(formatMemoryBlock)+ 进化状态 + 后台任务状态」再接揣测指令——揣测必须知道助手"是谁、记得什么、在忙什么",否则心理是猜的空气(用户提示词要求冷淡,猜"热情洋溢"就串味);块缺失/读取失败静默跳过,不杀揣测。渲染端 `mindGuess` 状态(localStorage `widget-agent-mind`)与标题共用**泛化标签 runner**(`createLabelRunner` 工厂:入口守卫/排队追平/1.5s 重试/10s 补跑,总结标题与心理揣测各自独立 in-flight 状态,行为与原 runSummary 逐句等价)。**主动陪伴(2026-08-07,用户要求"LLM 主动向用户发送消息")**:用户无操作满 N × 单位(设置 `agent.proactiveEnabled` 默认开 / `proactiveInterval` 默认 15(2026-08-08 用户要求,原 60),钳制 5–480 / `proactiveIntervalUnit` s·m·h 默认分钟;旧 `proactiveIntervalMinutes` 由 main.cjs `currentAgentConfig` 统一迁移,数值不变单位 = 分钟)后,**增强的总结标题 Sub Agent 独立判断语境是否需要主动开口**(`createSummaryAgent.judgeProactive`——它已有总结上下文的能力):JSON 输出 `{should, hint?}`,`parseJudgeJson` 严格解析(复用 `extractJsonObject` 候选链:代码块/前缀/尾随/Python 单引号 dict),失败/无 Key → should:false(安全侧,一次都不主动打扰);判断上下文与主引擎**同源**(自定义提示词 + 记忆块 + 进化状态 + 后台状态 + 当前时间,`buildJudgeSystem` 拼装),noThinking 低强度 60s 超时单措辞一次重试。**工具积极性强化(2026-08-10 用户要求"更拟人,人是会用工具的")**:① 判断措辞新增"值得用工具动手/查证的事"判据——后台任务状态块中有等待人工操作的任务(扫码登录)或进行中下载/转换时尤其适合开口、话题需要实时信息(可 web_search 查证后再开口,不凭空猜)、可用灵动岛设置工具帮用户改善体验(抱怨过字小/背景看不清);hint 可包含"该用什么工具做什么",让主动开口是行动而非纯问候;② PROACTIVE_INSTRUCTION 增补"像真实的朋友那样——人是会用工具的:需要真实信息就主动调用工具查证或顺手把事办了(web_search/查后台任务状态/灵动岛设置工具),不要凭空猜测;但不要为了用工具而用工具,把话说短说自然,行动融入对话而不是罗列工具"。**判断为"是"→ 主 Agent 完整回合主动回复**(`engine.proactiveTurn(history, {hint})`):思考/流式/工具/子代理全保留,消息以正常助手气泡流式落定(message 事件带 `proactive: true` 标记——渲染端据此重置 idle 时钟防触发循环);内部指令为 role:'system' 请求项追加在 **input 末尾**(三 provider 序列化器补 system 分支:Responses `{type:'message', role:'system', content:[input_text]}` / Chat `{role:'system'}` / Anthropic 并入 user 文本块,漏一处指令被当 assistant 输出角色错乱),**不进渲染端历史、不拼进 system prompt**(动态段拼进去断缓存前缀,50 倍价差);与 send 共享 running 互斥,busy 时**静默拒绝**不排队(不发 error 事件,被用户操作挤掉是正常),共享 ctl abort 链路(用户点"停止"可中止),确认门行为不变。**系统消息 = Windows 系统通知**(用户明确:不是对话内气泡):主动回合 message 落定后主进程 `getMindAgent().guess([该消息])` → `new Notification({title:'岛灵 · 心理揣测', body})` 展示 + `mind-proactive` 事件(`{type:'mind-proactive', messageId, guess}`)送渲染端更新紧凑态文字区——**同一句,两处一致、不重复调用 LLM**;渲染端标签 effect 对 proactive 末条**跳过 mindRunner**(心理由主进程统一提供),标题照常跟随。**调度器在渲染端**(useAgent,每 60s 检查:agent 模式(`useAgent({allowProactive: mode==='agent'})`)/ 配置开启 / status idle / 有历史 / 距上次"有操作"≥N 分钟 / in-flight 守卫)→ `agent:proactive-tick(messages, idleMinutes)`(IPC **resolve 时机 = judge 完成后**,渲染端 in-flight 覆盖 judge 全程,期间用户 send 天然优先——judge 结果到来时引擎已 busy 自动放弃);**judge-no 回退 idle 时钟**(下次判断在 N 分钟后,防闲置时每分钟一次 LLM 判断调用 = 1440 次/天);"有操作" = 用户 send / 清空 / 切换会话 / 主动回复落定。**AgentSettingsView「主动陪伴」区**(行为与界面菜单内):开关 + **关闭时折叠隐藏间隔设置**(2026-08-07 用户要求——`.island-proactive-config` grid-rows 0fr↔1fr 折叠/展开动画 0.36s 无过冲 + 透明度,内层 div 承担 overflow hidden 与 padding,防 0fr 轨道残留 padding 的坑;巡检段 2 断言折叠/展开往返)+ **触发间隔与单位两个独立 QuickMenu 按钮**(2026-08-08 用户要求,QuickMenu 化:间隔默认 15,预设 15/30/60/120 + 自定义参考输出预算按钮点击内联输入(5-480 钳制);单位默认分钟,秒/分钟/小时,**数值不变仅换单位**——用户约定;保存按 数值 × 单位换算 写 `proactiveInterval` + `proactiveIntervalUnit`;旧 `proactiveIntervalMinutes` 由主进程迁移;原 ScaleStepper 步进器随 QuickMenu 化删除)。**LLM 自我配置工具 `set_proactive_config`**(2026-08-07,configTools.ts):enabled 开关 / interval 间隔(5-480 钳制)/ unit 单位(s·m·h 枚举),对话里"关闭主动陪伴""把间隔改成 10 分钟"即可。**Windows 通知 Bug 修复(2026-08-07,用户实测右下角不弹)**:main.cjs 从未调用 `app.setAppUserModelId()`——Windows 上不设置 AppUserModelID,`new Notification().show()` 静默失败(进化/notify 通知同样受影响);whenReady 开头补 `app.setAppUserModelId('com.dynamic-island.widget')`。巡检段 4.8:注入 proactive message 事件(文本 ≤10 字防段 5 自动回复误判)+ 临时 stub `Notification.prototype.show` 捕获 → 断言气泡落定 / 系统通知弹出 / 紧凑态文字区与通知同一句;备份恢复 `widget-agent-messages`/`widget-agent-mind`/`widget-agent-sessions` 三键,不 reload。**巡检段 4.9:10 秒真实调度链路**(2026-08-07,用户要求"测试主动陪伴时间隔改为 10 秒"):agentSetConfig 写 10 秒 → 进 agent-settings 触发 useAgent onRefresh(config 重读)→ 返回长按展开回聊天 → 渲染端调度器(60s 周期检查,idle>10s)发 tick → judge(真实 LLM)→ should 则主 Agent 完整回合 → 消息落定 → 钩子 → 揣测通知;**调度触发判定不依赖 judge 结果**——main.cjs tick handler 记录 `lastProactiveTick`(deps 注入 getLastProactiveTick 轮询,judge-no 也证明调度链路通了);stub 通知捕获 + localStorage 揣测同步 + 历史末条 assistant 文本断言;配置改动由 settingsBackup 恢复兜底。**Sub Agent 设置(2026-08-07,Agent 设置第 5 组菜单「Sub Agent」)**:总结标题**文风**(summaryStyle)与心理揣测**人格**(mindPersona),各 **4 种预设 + 自定义 ≤100 字**(共 8 种预设:文风 concise简洁明了/lively活泼俏皮/literary文艺诗意/formal正式稳重;人格 catgirl俏皮猫娘/tender温柔贴心/aloof高冷克制/witty知性风趣)——预设表 `SUMMARY_STYLES`/`MIND_PERSONAS` 定义在 **constants.ts**(零 node 依赖,渲染端设置界面与引擎共用,引擎 subagents.ts re-export);配置存预设 id 或自定义文本,`resolveSubAgentStyle` 解析为提示词片段注入系统提示(文风拼进总结三级措辞、人格拼进 buildMindSystem 上下文),设置界面 **QuickMenu 档位(2026-08-08:默认 = 默认,预设 4 项,滚轮逐格切换)+ 自定义输入**(输入即切自定义,预设选中时输入框显示空,自定义文本不在档位时按钮显示文本、无高亮);**LLM 自我配置工具 `set_sub_agent_config`**(configTools.ts):summaryStyle/mindPersona 参数,预设 id 或自定义(截 100),空串恢复默认。**文字区回退 Bug 修复(2026-08-07,用户实测"有时展示 LLM 回复前几个字")**:loadSession 清 mindGuess 且 skipNextLabelRef 跳过生成 → 紧凑态文字区回退"最后回复预览";修复:skip 块**只跳过总结标题,心理揣测照跑**(重新生成,文字区恢复揣测优先)。**每轮回复完成后自动总结**(message 事件落定即触发,后台静默):结果存 `currentTitle`(localStorage `widget-agent-title` 持久化),**紧凑态文字区 idle 时按 心理揣测 → 标题 → 最后回复预览 优先显示**(2026-08-07 起心理揣测在前,标题仍入历史作会话标题);新对话入历史直接用 currentTitle 作标题(无需再调 LLM)。**会话版本号防竞态**(`sessionVersionRef`):**仅在 clear/loadSession(会话真正切换)时递增**——send 递增会把每轮总结全部作废,连续对话时标题永远等不到(实测 bug:文字区一直显示回复预览回退);总结触发为 effect(messages 落定 + status idle 后),**排队追平**(in-flight 时标记 pending,完成后补跑最新一轮——跳过是连续对话总结失败的常见原因);**runSummary 入口守卫**(任何路径不并发总结)且 in-flight 标记跨越重试窗口(否则重试期新总结并发、陈旧快照后完成会覆盖新标题);失败重试:1.5s 后同快照重试(retryLeft 预算 1),预算耗尽 10s 后**补跑一次最新消息**(仅此一次不连锁——会话静止时标题也能补上,不会永久停留在回复开头预览;补跑也不再触发新总结),loadSession 跳过下次总结;旧快照结果直接生效(主题一致,新总结覆盖)。**summarize 输入裁剪最近 12 条**(总结主题只需近期对话;完整历史含大量工具结果会拖慢/超时)。**web_search 必须 Bing 主用、DDG 回退**(duckduckgo 在中国不可达,实测 fetch failed;Bing 解析 `li.b_algo` 的 h2 链接 + p 摘要)。**上下文硬盘缓存(DeepSeek 自动开启,无需配置)**:请求前缀**完整匹配缓存前缀单元**才命中(非最长前缀匹配;单元来自请求结束位置/公共前缀检测/固定 token 间隔落盘);多轮对话天然命中(完整历史回传 = 前缀递增);**前缀必须稳定**——instructions 与历史序列化幂等、tools 顺序固定、reasoning item(Responses)/ reasoning_content 字段(Chat)固定回传,任何序列化抖动都会断缓存。usage 透传缓存命中:deepseek `input_tokens_details.cached_tokens`(Responses 默认)/ anthropic `cache_read_input_tokens` → message 事件 usage.cached → AgentMessage.usage 持久化,助手消息尾部显示"输入/输出/缓存命中"小字(可观测命中率)。**官方文档对照项**:deepseek-v4-flash 上下文 1M(超出 400)、输出上限 384K、思考模式默认开启(effort 可配)、缓存命中价 0.02元 vs 未命中 1元(50 倍,缓存即省钱)、并发 2500(不会撞 429)、峰谷定价(9-12/14-18 时双倍)、流式 keep-alive 注释(已正确跳过)。**预算治理**:`trimHistory` 按 token 粗估(中 1/字、英 4 字符/token,系数 0.6)裁剪历史,上限 400K(2026-08-08 用户要求从 200K 提升,远低于 1M 窗口)、至少留 10 条,仅超限触发(不断缓存前缀);**max_output_tokens: 8192 动态可调**(2026-08-08:主对话/子代理循环,含思维链 token——原 4096 在思考模式高 effort 下常被思维链吃光,工具调用参数被截断成空串 → 空参调用死循环,用户实测;修复后按用户要求"默认砍半"从 16384 → 8192——预算只是上限,回复不会因此变长;总结/判断等无思考短任务保持 4096 缺省;streamResponse/streamChatCompletion/streamAnthropic 增 `maxOutputTokens` 参数;Chat 路径为 max_tokens)。**LLM 自主调整(2026-08-08 用户要求"LLM 自主判定要多少预算")**:引擎持有**可变预算** `outputBudget`(初始 = 持久化配置 `agent.maxOutputTokens` ?? 8192,越界回退;AgentEngine 暴露只读 getter 供测试/UI),主循环与子代理每轮传当前值;工具 **`set_output_budget`**(engine 注册,**action 模式**):`action=get`(查当前预算/缺省/范围,供决策,不改状态)或 `action=set`(`maxOutputTokens` 钳制 4096–262144 + `persist` 缺省 false = 仅本次会话,true = 经 updateAgentConfig 写 settings.json)——**按任务实际需要设值,不是越大越好**(描述与返回都引导:大任务设合理目标值如 32768/65536,完成后调回缺省,避免失控回复烧 token);**预算不足提示(用户要求)**:provider 报告响应被截断(Responses `response.incomplete` / Chat `finish_reason='length'` / Anthropic `stop_reason='max_tokens'`)→ ProviderOutcome.truncated → 引擎**不落定半截回复**(原逻辑 calls 为空直接落定 = 提示永远用不上,实测),已输出文本入历史 + 注入 `BUDGET_TRUNCATE_HINT` system 提示(每回合一次,不进渲染端历史):LLM 自主判断——任务需要更长输出 → set_output_budget 按需调大后续写;已基本完成 → 收尾;子代理循环同款;main.cjs `applyAgentConfigPatch` 加数字字段钳制(与 proactiveInterval 同款);渲染端 Agent 设置「连接」组新增**输出预算输入**(4096-262144,缺省 8192,保存经 agentSetConfig;对话中 LLM 调预算不影响表单显示,persist 后重进设置可见);**reasoningEffort 可配置**(none/low/medium/high,默认 high,设置页"思考强度"新增"关";2026-08-08 QuickMenu 化:关/低(快)/中/高(深)档位滚轮切换)。**tools.ts 的 fs 只用 `promises as fs`,existsSync/mkdirSync 在主模块**(`import { existsSync, mkdirSync, promises as fs }`——promises 没有 existsSync/mkdirSync,实测报错)。**并行工具执行**:一轮多个工具调用 `Promise.all` 并发(DeepSeek 并行工具调用始终开启;结果按调用顺序回填,UI 卡片顺序一致;每个独立 60s 超时;抽共享 `executeToolBatch`)。**delegate 子代理工具**:嵌套 agent 循环(独立上下文,`task` 必填 / `system` 可选专用提示 / `tools` 可选工具子集),事件静默(reasoning 仍累积满足回传要求),返回结果文本;LLM 一次发多个 delegate 即并行子代理;主循环与子代理共用 executeToolBatch。
- **通用后台任务注册表**(`electron/agent/tasks.ts`,2026-08-07,零 node 依赖纯 TS):把 bili 的后台设计(生成二维码 → 后台轮询 → 完成发通知 + background-done 自动触发对话)**泛化**为引擎级机制——任何需要人工介入/后台推进的任务(扫码登录、人工确认、下载……)注册 `{id, title, status: waiting|running|done|failed|cancelled, detail}` 后:① **状态实时注入系统提示**(`getTasksStatusBlock`,替代原 bili 专用 `getBiliBackgroundStatus`,engine/subagents 三处共用;文案稳定不破坏前缀缓存,含「等待人工操作的任务要提醒用户当前需要做什么」指导);② **进入终态触发 done 回调一次**(`updateTask` 终态后忽略再更新;已终态任务 TTL 24h 清理,进行中 6h 失联标注)——createTools 接线 `setTaskDoneHandler` → background-done → 渲染端自动触发一轮对话,LLM 主动告知用户结果(**反馈空间**;完成/失败/取消都有,不再"通知完就结束、LLM 无感知")。bili 迁移:登录注册「B站扫码登录」waiting(独立任务 id `bili-login-<key>`,重新扫码不互相覆盖),成功 done/失败 failed 都有对话反馈(原失败只弹通知);下载注册「B站下载」running(detail 带进程与输出目录),完成/失败进终态——**顺带修复:下载完成此前从未触发 background-done**(runBiliBackground 的 onDone 参数从未被传入,对话里 LLM 永远不知道下载结果,只有系统通知)。工具如需复用:导入 tasks.ts 的 registerTask/updateTask 即可。
- **MCP 服务接入**(`electron/agent/mcp.ts`,零第三方依赖手写客户端,**双传输**——参考 opencode 源码 internal/llm/agent/mcp-tools.go 的设计:配置同构 type/command/args/env/url/headers;差异:opencode 每次调用独立连接,本项目为**常驻进程/流复用**(连接一次反复调用,崩溃自动重启,调用间零握手开销)。**stdio 传输**:JSON-RPC 2.0、stdin/stdout **每行一条消息**(非 LSP 的 Content-Length 帧);**sse 传输**:GET 端点建立事件流(endpoint 事件给出 POST 回传端点,通常带 session),请求 POST 回传端点、响应经同一事件流按 id 推送(ping 事件忽略,兼容响应体直接返回的实现)。握手统一:initialize(请求 2024-11-05,兼容面最广)→ notifications/initialized → tools/list;调用 tools/call。服务端主动日志(notifications/message)打日志,服务端发来的请求回 Method not found(-32601),非 JSON 输出行忽略。**工具命名 mcp_<服务名>_<工具名>**(仅 [a-z0-9_],中文/符号转写下划线;重名加序号);inputSchema → parameters(非 object 模式包一层 input 字段);结果 content 文本块拼接、图片/二进制资源只标注大小不塞 base64、截 8000;服务端 isError → 抛错按"工具执行失败"回填(LLM 可自纠)。**生命周期**:每服务一个常驻连接(stdio 子进程 / sse 事件流,崩溃或断流后下次调用自动重启);配置增删改即销毁旧客户端(prune 按 key = 全字段 JSON 判定);**连接互斥 connectPromise**——并行工具调用(Promise.all)/delegate 子代理与主循环并发会对同一服务并发 connect,不加锁会拉起两个进程、旧进程泄漏挂起(实测隐患);连接失败在 listTools 静默跳过(返回空,不影响对话,真正调用到该工具才报错);60s 超时由引擎 executeToolBatch 兜底,JSON-RPC 层 55s。**Windows cmd 宿主**:npx/npm 等 .cmd 命令必须经 `cmd.exe /d /c` 启动(CreateProcess 无法直接执行 .cmd;规则:命令不以 .exe 结尾一律走 cmd 宿主);cmd /c 会把参数里的 `%VAR%` 当环境变量展开,路径含 % 需加引号。**销毁**:Windows 走 `taskkill /pid /T /F` 连进程树(直接 kill 只杀 cmd 宿主,node 子进程残留;进程是自己启动的,杀树安全);before-quit → `agentEngine.dispose()`(懒加载:从未用过不会创建引擎,无泄漏)。配置 = settings.json `agent.mcpServers`(name/type/command/args/env/url/headers,main.cjs applyAgentConfigPatch 结构化校验:字段长度与数量上限);设置视图"Agent 设置"MCP 服务编辑(**传输类型切换** stdio/sse,参数/环境变量每行一个,保存时转换)+ 逐条「测试」按钮(IPC `agent:mcp-test`)。工具列表视图(agent:tools)会显示 MCP 工具。
- **技能接入**(`electron/agent/skills.ts`):扫描 skillsDirs(Claude Code 技能约定:目录内含 SKILL.md,frontmatter name/description + 正文使用文档);**默认扫描源**(存在才注册,main.cjs DEFAULT_SKILLS_DIRS,设置里可增删):`~/.claude/skills`(Claude Code)、`~/.codex/skills`(Codex)、`~/.config/opencode/skills` 与 `~/.config/opencode/plugins/skills`(opencode)、`userData/skills`(挂件自有技能目录)。每个技能注册为 `skill_<slug>` 工具(slug 仅 [a-z0-9-],中文名回退目录名,重名加序号):**调用时把 SKILL.md 全文注入上下文**(截 8000 带"已截断"标注)+ 技能目录绝对路径(附带脚本用 exec_command 在目录下运行);description = frontmatter description 压单行截 300。每次 listTools 实时重新扫描(配置变更即时生效,本地读取开销可忽略)。引擎每轮循环开始前拉取外部工具列表(getExternalTools:MCP + 技能,失败静默跳过),**delegate 子代理继承外部工具**(tools 参数限制子集时只给列出的);技能工具无参数。**技能排除**(2026-08-06):`agent.excludedSkills`(slug 数组)——被排除的技能**扫描跳过**(listTools 第二参数,候选/工具列表不再出现);支持两条路径:① LLM 对话 `skills_config` 工具加 `exclude`(校验技能确实注册)/`include` 恢复,list 显示全部技能含排除标注;② 设置界面技能列表每行**移除**按钮(即时生效,已排除区可**恢复**;本地 state + agentSetConfig 提交,引擎下一轮生效)。createConfigTools 的 `listSkills` 依赖注入(engine 闭包 skillLoader,exclude 校验用;模块级工厂不直接引用闭包变量)。**自然语言创建技能**(2026-08-06):`skills_config` 加 `create` action——LLM 提供 name/description/content,引擎规范化写入 `userData/skills/<slug>/SKILL.md`(默认扫描源之一,下一轮起 /技能名 可用);frontmatter 由引擎组装(name slug 化仅 ASCII/description 压单行 ≤300/content ≤50000);同名冲突检查基于**实时扫描**(listSkills 注入,创建后立即可见,非静态清单),`overwrite=true` 可覆盖;`getSkillDir` 经 EngineDeps 注入(main.cjs → userData/skills)。真实 LLM 验证:对话"帮我创建一个写诗技能" → LLM 调 create → poem-writer/SKILL.md 落盘、frontmatter 规范完整。**技能分区**(2026-08-06):`skills.ts` listTools 第三参数 `ownDirs`(引擎传 userData/skills)——技能父目录在其中标记 `sourceKind: 'own'`(自己创建),否则 `'scanned'`(扫描到的外部技能;比较大小写不敏感);AgentTool/AgentToolInfo/listAllTools 透传;设置技能区**分区展示**:「自己创建(N)」在前、「扫描到的(N)」在后(各自计数与移除按钮),空区不显示;无自建技能时只有"扫描到的"区。UI 验证:预置 userData/skills 技能 → 进入「自己创建(1)」区,外部 10 个在「扫描到的(10)」区。
- **IPC**:渲染端 `agent:send(text, history)` / `agent:abort`;引擎事件经 `agent:event` 转发(webContents.send);`agent:config-get/set` 读写 settings.json 的 `agent` 段(apiKey/baseURL/model/systemPrompt/**mcpServers/skillsDirs**,字符串与数组字段分开校验,共用 `applyAgentConfigPatch`——LLM 自我配置工具也走它);`agent:tools` **异步**(listAllTools = 内置 + MCP + 技能,UI 工具列表视图用);`agent:mcp-test`(测试 MCP 服务连通,独立连接→列工具→销毁);`agent:memory-get/set`(记忆管理器:add/remove/update/replaceAll,返回最新列表);**`agent:memory-export`**(记忆导出:保存对话框 → 写 memory.json 完整 JSON,结构同文件可再导入);**`agent:memory-import`**(记忆导入,2026-08-06:打开对话框选导出文件 → 校验(结构同 memory.json 的 `{entries:[...]}`,兼容纯数组)→ 规范化(类型校验/内容截 500/时间戳兜底)→ `store.importEntries` 合并去重,返回 {imported, skipped} 计数);`agent:evolve`(触发记忆进化,后台)/ `agent:evolution-log` / `agent:evolution-rollback`(进化日志与回滚)。**配置刷新**(2026-08-05):useAgent 的 config 只在挂载时读一次——LLM 对话中 mcp_config/skills_config 写的配置,设置界面看不到(实测 bug:对话里配了 siyuan MCP,设置里为空)。修复:`useAgent.refreshConfig()`(重新 agentGetConfig);**在 DynamicIsland 切到 agent-settings 视图前触发**(panelView effect,agentConfig 经 ref 访问防对象字面量新引用导致无限循环)——**不能在 AgentSettingsView 挂载后刷新**:异步返回的 config 更新会触发填充 effect,在用户编辑表单时重置表单、丢失编辑(实测:正在填的 MCP 服务被清空)。设置界面记忆区新增**导出按钮**(agent:memory-export,记忆为空时禁用)。**设置视图统一动画**(2026-08-06,用户要求:精致缓动优雅、速率不快、轻微回弹):`island-ui-in`(淡入+上移+微缩放,0.34s `cubic-bezier(0.34,1.56,0.64,1)` 回弹)/`island-ui-out`(离场 0.24s 平滑)/按钮 `:active` 缩放回弹——应用:MCP 卡片挂载、记忆条目挂载/删除(leaving 状态机:先播 260ms 离场动画再真正 IPC 删除)、技能行挂载/移除(同 leaving 机制,移除技能动画后才提交排除)、进化日志;**keyframes 缺失 Bug 修复(2026-08-07)**:`@keyframes island-ui-in/out` 从未在仓库定义(base.css 补上,淡入+上移+微缩放 / 上移淡出)——此前所有 `animation: island-ui-in/out` 引用静默失效(CSS 引用不存在的 keyframes 不报错,动画不播放),记忆/进化/技能/设置内容切换的入场离场全部没播,用户反馈"没有切换动画"才暴露行与结果提示挂载、空态初始化提示;**保存反馈徽标**(SavedBadge,保存配置/保存歌词 API 共用:绿色对勾 SVG 描线动画 + 回弹淡入、隐藏平滑退场,绝对定位不挤动保存行布局——替代原纯文本「已保存 ✓」,2026-08-06);进化区新增**「清除所有版本」**(`agent:evolution-reset` → `resetAll()`:删快照目录/日志/state,回 v1 初始状态,记忆保留;清空后显示"暂无进化记录"初始化空态)。**技能导入**(`agent:skill-import`):dialog 选择**技能包文件夹**(含 SKILL.md 与脚本等,`fs.cpSync` 整目录复制,排除 .git)或**单个 .md 文件**(提取 frontmatter name 或文件名作 slug)→ 复制到 userData/skills 并写 **`.island-imported` 标记**(手动导入区标识);结果返回 imported/skipped 列表。**导入后立即显示**(2026-08-06 Bug 修复):导入成功即 `agentGetTools` 刷新本地快照(localTools),技能马上出现在"手动导入"区——此前提示"重进可见",用户停留在视图看不到。**技能三区**(2026-08-06):`sourceKind` 扩展 `created`(灵动岛创建:userData/skills 无导入标记)/ `imported`(手动导入:目录有 .island-imported)/ `scanned`(外部目录);设置技能区三区展示(灵动岛创建 / 手动导入 / 扫描到的,各自计数与移除)。**MCP 服务分组**(2026-08-06):按服务名主段(第一个字母数字串)归组——同一程序的多个服务(如 siyuan-assets/siyuan-notes)归一个大类,组头(名称+数量+折叠箭头)点击折叠(组体 max-height 过渡,0.32s 缓动)。**记忆类型定制下拉**(2026-08-06):替代原生 select——按钮显示类型徽标+箭头,点击展开选项浮层(回弹滑入)/点击外或选择后收起动画(平滑退场)。**本体滚轮切换**(2026-08-06):直接在类型按钮上滚动逐格循环切换类型(与快捷按钮共用 `useWheelSteps` 钩子——每 60px 一步/步间 ≥100ms/350ms 无滚动重置,手感一致),下拉展开时不响应(选项浮层是当时的交互面);滚轮每格重挂载按钮(`key={tick}`)经 `WheelSwap` 重放内容交换动画(旧徽标滑出淡出/新徽标回弹滑入,方向随滚轮),**`.tick` 类仅在滚轮触发后加**(首帧挂载不加类,设置视图进入时不闪动;flash 用 `agent-quick-flash-dim` keyframes,基色 0.14 与按钮边框一致,强调色 45% 混色柔和强调);行徽标(记忆条目上的类型)不随动。**编辑记忆动画**:编辑态容器挂载回弹动画,保存/取消退出恢复行显示。**删除 MCP 服务动画**:卡片离场(leaving 260ms 后移除)。**默认界面缩放 200%**(2026-08-06):无 localStorage 时默认 200(初次安装初次进入 Agent 模式即放大;用户改过保留)。**对话历史视图**(2026-08-06):与工具列表相同设计——`measureHeight` 守卫扩展到 history(岛体保持聊天高度,列表在剩余空间滚动,实时响应布局)。
- **记忆系统**(`electron/agent/memory.ts`,2026-08-05 新增,取代单一"系统提示词"):结构化长期记忆存 **userData/memory.json**(与 settings.json 分离:高频变更不污染配置,损坏不影响配置),条目 = `{id, type: preference|fact|workflow|lesson, content, source: manual|agent|evolution, createdAt, updatedAt}`,上限 200 条、单条 500 字,写盘串行队列防并行竞态。**系统提示拼装**:`自定义提示词 + 记忆块 + 进化状态 + 后台任务状态`(记忆块按类型分组、截 6000,静态段——变更才断缓存前缀)。**LLM 记忆工具**:remember(沉淀,自动去重)/ forget / list_memory / update_memory——对话中"记住:…"即写长期记忆;**createMemoryTools 收惰性 getter(2026-08-10)**:execute 时实时 `getStore()`,引擎 tools 创建时组装一次不能捕获固定实例(清除数据重建 store 后旧引用操作已删记忆、与渲染端永久不一致,见数据管理段);engine.ts 的 getMemoryStore() 同样实时获取(getMemoryBlock 与工具组装共用)。**设置视图记忆管理器**:条目列表(类型徽标 + 内容省略 + 改/删)、添加行(类型选择 + 内容)、对话沉淀与人工编辑同库。**导入/导出**(2026-08-06):导出 = 保存对话框写完整 JSON(可再导入);导入 = 打开对话框选文件 → `store.importEntries` 合并——按 **id 与内容**去重(已存在跳过)、导入条目 updatedAt 置为当前(列表倒序置顶可见)、总量超 200 淘汰最旧(与 add 一致,新导入总是保留);UI 导入按钮显示「已导入 N 条,跳过 M 条(已存在)」并立即刷新列表。
- **自我进化 harness**(`electron/agent/evolution.ts`,按 [penguin-harness](https://github.com/Prism-Shadow/penguin-harness) 的 agent-optimization 技能与 snapshot-service 重构:本地参考仓库 `C:/Users/asus/Desktop/penguin-harness`)。**评估委托独立 Sub Agent**(2026-08-05,借鉴 penguin"评估必须委托 agent-evaluation 子代理,优化器自己不评分"语义):`createEvaluatorAgent()` 独立实例,评审/复评都经 `evaluator.evaluate()` 执行——独立 AbortController/60s 超时/失败自动重试一次/事件静默(仅 evolution-progress 阶段提示),优化流程自身不直接调 LLM 评分。**版本化快照**:每个**接受**的版本存档 `userData/memory-snapshots/v<N>.json`(同版本不重复打包,借鉴 ensureSnapshot);`memory-state.json` 持久化当前版本号/评分;**回滚只到已接受版本**(防降级——拒绝的候选从不产生版本)。**Reference 语义**:当前已接受版本 = Reference,每轮从它构造一个候选,接受后成为新 Reference。**假说驱动**:评审的每条改进建议必须带 `hypothesis`(预测的可观察行为变化),无假说的建议一律不采纳("只加分析不预测行为的改进"是无效改进)——评估侧只给公开记忆内容,黑盒打分防自评偏差。**多轮候选循环**:每轮 评审(独立 LLM 调用,rubric = 冗余/一致/时效/可操作/价值,总分 0-100)→ 确保 Reference 快照 → 应用候选 → 复评(独立调用)→ 棘轮(新分**严格高于**原分才接受:版本+1、立即存档、更新 state;否则从快照恢复)——轮数预算 rounds(evolve_memory 工具参数,默认 2 上限 4),评分 ≥92 达标提前停,LLM 调用失败不消耗轮数;被拒结果作 evidence 记录日志。**CONTRACT**:进化只改记忆(可编辑资产),不触碰引擎/工具代码。**后台任务语义**(借鉴 bili 下载):工具/设置按钮触发后立即返回,完成发系统通知 + **状态注入系统提示**(getStatus 块,LLM 对话中可感知进化结果);`evolution-progress`/`evolution-done` 事件渲染端忽略(状态机不受影响);日志 evolution.json(上限 20 条,含版本号,设置界面展示 + 「回滚到上一版本」)。provider 分发抽到 `electron/agent/provider.ts`(engine 与 evolution 共用,避免循环依赖;esbuild 入口仍是 engine.ts,`export { createEvolution } from './evolution'` 打进同一 agent.cjs)。
- **LLM 自然语言自我配置**(mcp_config / skills_config 工具):对话中说"添加一个 MCP 服务:…"即可自我配置——list/add/remove/test(MCP)、list/add/remove(技能目录),写配置经 deps `updateAgentConfig`(main.cjs 注入,复用 applyAgentConfigPatch 校验);**新增服务/目录下一轮对话起生效**(工具清单每轮刷新),结果文本里注明。
- **手动调用**(/ 与 @):输入以 `/` 开头 = 调技能(如 `/trump-perspective`,匹配完整名/去前缀/模糊唯一命中),以 `@` 开头 = 调 MCP 工具(如 `@mcp_filesystem_read_file`);引擎在循环前先执行工具,剩余文本是合法 JSON 对象则作参数,结果以 tool-call/tool-result parts 入历史(事件照常转发,UI 工具卡片一致),LLM 基于结果直接回复;多命中/未找到给可读提示。**输入框候选列表**(2026-08-05):输入 `/` 或 `@` 时浮出匹配的技能/MCP 工具列表(输入框上方,`island-agent-suggest`,最多 6 条,按输入 token 过滤)——↑↓ 导航 / Enter 选中(替换前缀命令,不发送)/ Esc 关闭 / 点击选中;数据源 = agent.tools 过滤:**技能 = `skill_` 前缀;MCP = `mcp_` 前缀且名称含第二个下划线**(内置工具 mcp_config 恰好以 mcp_ 开头,用双下划线区分——外部 MCP 工具名必有 `mcp_<服务>_<工具>` 两段,实测 @mcp_config 混入候选)。
- **四路架构审计落地(2026-08-07,四代理并行审计渲染端/主进程/引擎/类型 + 自主实施;主进程轮)**:
  - **确认门并行竞态修复(P0,对话永久挂死)**:`confirmCommand` 单槽被并行 exec_command(executeToolBatch 是 Promise.all)覆盖时,旧 promise 的 120s 定时器触发时槽已空 → 无人 resolve → 整轮冻结且 agent:abort 解不开。修复:新请求到达**立即以拒绝落定旧请求**(引擎结构化回填 LLM 可自纠)+ 定时器闭包只处理自己那次(`=== slot`)。
  - **巡检配置残留修复(P0)**:测试「保存配置」改了内存 settingsCache,磁盘恢复对缓存无效——退出瞬间 before-quit flushSettings 用旧缓存覆盖刚恢复的文件(残留 confirmExec 开关/ui-mock 服务)。修复:`resetSettingsCache()`(置 null,flushSettings 对 null 缓存 no-op)注入 runScreenshotTests deps,恢复文件后调用。
  - **IPC 错误处理统一(P1)**:`safeHandle(channel, fn)` 包装器,evolution-rollback/reset/tools 三个可抛 fs 错误的 handler 统一 {error} 返回(渲染端 rollback/reset 展示错误文本,不再 unhandled rejection);memory-export 文件不存在 → 导出空 {entries:[]}(原 ENOENT 抛错)。
  - **desktop.d.ts 与 preload 漂移修复(P1,3 处)**:onOpenSettings/onOpenHelp 类型补为返回取消订阅函数 → WidgetApp 接住返回值 cleanup(修 StrictMode 双挂载回调翻倍);补 openExternal(删 Markdown.tsx 局部类型 hack);agentGetConfig 补 confirmExec、agentSetConfig patch 补 confirmExec、agentGetTools 补 sourceKind、rollback/reset 补 {error} 返回。
  - **安全与健壮性(P2)**:runIslandSettings 操作**白名单**(13 个 IslandSettingsOp,防 constructor/__proto__ 原型链键命中);引擎事件转发统一 `sendToWidget`(isDestroyed 守卫);system-media-bridge requestPS 写前判进程存活 + ps.stdin error 兜底(防崩溃重启间隙 EPIPE 杀死桥进程);screenshot-tests 删无效的 sloppy-mode `quitting = true` 全局赋值。
  - **文档修正(P1,两审计独立确认)**:CLAUDE.md「双岛并存模式」章节**标注为设计文档(未实现)**——全仓库对 dual 零匹配,main.cjs 的 widget:set-mode 只接受 music/agent、托盘 radio 两项、无 dual-shot.cjs。
  - **tsconfig 小项**:vite.widget.config.ts 纳入 tsconfig.node.json(补 `tsc -b` 盲区);tsconfig.electron.json 删重复 "types" 键。
  - **二轮(推荐项落地)**:
    - **engine.ts 拆分**(审计 P1,1291 → 763 行):总结标题/心理揣测 Sub Agent 全家 + 相关纯函数(compressArgs/sanitizeTitle/fallbackTitle/parseTitleJson/extractJsonTitle/looksLikeCodeLiteral/sanitizeMind/buildMindSystem/createSummaryAgent/createMindAgent)→ **electron/agent/subagents.ts**(383 行,依赖仅 provider/types/memory/tools);`createConfigTools`(224 行)→ **electron/agent/configTools.ts**;engine.ts re-export 保持测试/main.cjs 导入路径零破坏。拆前用 diff 逐字节核对搬移块(曾误删 trimHistory/确认门,从备份恢复后按精确边界重删)。
    - **provider 公共层**(审计 P1):parseSse 原 deepseek/chat/anthropic 三份逐字重复 → **electron/agent/sse.ts** 单一实现(yield SseFrame {type, data});8000 截断 ×3 → `truncateResult`;删 chat.ts **死副本 parseToolArgs**(与 deepseek 逐字节相同且零引用)。注意 chat 循环原直接 yield 裸对象,适配为 `evt.data`。
    - **useWheelSwap hook**(审计 P1,4 处 tick/prev/dir 状态舞蹈收敛):快捷按钮(AgentView)/记忆类型按钮 + 缩放步进器(AgentSettingsView)/帮助手册模式按钮(SettingsViews)→ `src/hooks/useWheelSwap.ts`,`step(oldValue, direction)` 只管动画状态,值由消费方更新。
    - **settings-store.cjs**(审计 P1,设置持久化可单测):main.cjs 的 loadSettings/saveSettings/flushSettings/resetSettingsCache/settingsPath(~80 行)→ **electron/settings-store.cjs** `createSettingsStore({safeStorage, getUserDataPath})` 工厂;main.cjs 留同名薄包装(17 处既有引用零改动)。测试(内存 safeStorage stub + 临时目录,`createRequire` 运行时加载):**原子写/防抖合帧/.bak 保留上次成功版本/损坏恢复/加密落盘无明文/解密读回/resetCache 后 flush no-op**——注意 .bak 只在主文件已存在时创建(首次落盘无 .bak),测试需两次落盘。
  - **三轮(高内聚低耦合 + 跨模块复用)**:
    - **垂直解耦(审计 P2 #11)**:新 **electron/agent/constants.ts**(零 node 依赖,渲染端可安全 import,与 types.ts 同款)——MCP 服务描述前缀 `MCP_SERVICE_LABEL_PREFIX` + `stripMcpServiceLabel`(引擎 mcp.ts 生成 / AgentView 候选列表剥除共用,原渲染端正则硬编码引擎格式,引擎改格式 UI 静默失效);`detectProvider` 从 provider.ts 移入 + 新增 `providerLabel`(设置界面协议提示与引擎分发共用,原 AgentSettingsView 三连问重实现)。provider.ts re-export 保持测试导入路径。
    - **useAgent 内部收敛**:`loadConfigAndTools`(挂载 effect 与 refreshConfig 逐字相同;注意声明须在挂载 effect 之前,否则 deps 数组 TDZ)+ `truncateCodepoints`(后台标签结果兜底截断 ×3)。
    - **useLeavingList hook**(审计 P2 #10,离场动画定时器模式 5 处收敛):会话历史行/工具禁用行(AgentView)+ MCP 卡片/技能行/记忆条目(AgentSettingsView);`beginLeave(id, commit)` 260ms 后提交 + 自动取消标记 + 卸载清理定时器。不入 hook 的:输入候选 stagger 退场(240+n×30 总时长/重复重置语义不同)、记忆编辑三段式(380+260)。
    - **conflictsWithBar**(layout.ts,conflicts 公式内联 ×4 收敛)。
    - **readAgentScale 导出**(settingsBridge,设置桥读现值与 useAgentPanelLayout 初始化共用,收敛两处独立 clamp)。
    - **技能扫描 TTL 缓存**(skills.ts,引擎 P2 #8):scanDirs 每步全量磁盘重扫(10 技能 × 10 步 ≈ 100 次文件读/轮)→ 按 dirs 键缓存 ~1s;excluded 只影响过滤不进键;「创建后下一轮生效」语义不变。
  - **四轮(高内聚低耦合续)**:
    - **AgentSettingsView 拆组件**(审计 P1 #7):`McpServerCard`(原 ~123 行 JSX 嵌在分组 map 里 4 层缩进,props 化:idx/server/testing/testResult/leaving + onTest/onRemove/onPatch)、`SkillsSection`(灵动岛创建/手动导入/扫描到的三区原同一区块复制 3 遍,仅标签与条目谓词不同)、`loadMemoryList`(挂载/导入/回滚/进化完成 4 处「agentMemoryGet().then(setMemory)」逐字重复)。
    - **AgentView 收敛**(审计 P2 #9):`scrollMessagesToBottom(el, {durationMs, blur})`(桌面挂件直接跳底/Web 平滑滚动两处重复)、`BackArrowIcon`(shared.tsx,返回箭头 SVG 三份逐字重复:BackButton + AgentView 两处)。
    - **死通道清理**(审计 P2-1):widget:hide / widget:quit / widget:topmost / widget:set-height 四个渲染端零调用的 IPC 通道删除(preload 暴露 + main handler + desktop.d.ts 同步);托盘退出走 app.quit、置顶走 tray 直调;窗口尺寸统一走 widget:set-size。
  - **五轮(高内聚低耦合续)**:
    - **tools 双通道收敛**(审计 P1 #4):agentConfigProp 去掉 tools(原 WidgetApp 同时经 agentConfig.tools 与 AgentPanelProps.tools 传同一 useAgent.tools),设置视图统一从 agent prop 取——单一来源,消"只更新一条通道静默漂移"。
    - **未跟踪定时器修复**(审计 P2 #13):useIslandMedia 循环模式 1.2s 跟随检测 setTimeout 卸载后仍 setState → cycleModeTimerRef 跟踪(连续点击重置)+ 卸载清理。
    - **smtc-bridge.cs 会话选择循环收敛**(审计 P2 #7):State/Control 两处逐字重复 → PickSession()。
    - **main.cjs asArray**(审计 P2 #7):agent:send/summarize/mind-guess 三处 Array.isArray 兜底收敛。
    - **过时注释修复**(审计 P2 #3):settingsBridge AGENT_SCALE_STORAGE_KEY 注释(原称"与 DynamicIsland 的 AGENT_SCALE_KEY 一致"——该常量不存在,实际反向导入)。
    - **面板视图 13 分支 if 链**:维持 CLAUDE.md「有意未做」结论(收益为可读性边际、风险为历史 bug 密集区),不再抽注册表。
  - **六轮(类型面清理 + 明确不做项)**:
    - **desktop.d.ts 类型补全**(审计 P2 #5):抽具名 `IslandAgentConfig`(agentGetConfig 返回与 agentSetConfig 返回共用,原 setConfig 返回 Promise<unknown> 需 `as AgentConfig` 强转);agentGetTools 的 parameters 补成 schema 形状(原 unknown 与 AgentToolInfo 不匹配)→ **useAgent/AgentSettingsView 三处 `as` 强转全部移除**(编译期类型即契约,desktop.d.ts 与 preload 实为同一事实来源)。
    - **明确不做**(均记录):App/WidgetApp 宿主接线工厂(需 ~25 个参数——比替换的 JSX 还难读,两端变量名不同,收益<风险)、状态文案双源合并(紧凑态流程文案 vs 头部状态徽标语义不同,强合加参数反降内聚)、面板视图注册表(见上)。
  - **引擎 P0 缺陷修复**:① delegate 子代理**继承主回合 abort 信号**——`runSubAgent` 增 signal 参数,每步 `AbortSignal.any([55s 超时, 主回合])`,循环顶/调用后检查提前 break,用户点"停止"后子代理不再烧 token;确认门改为子代理启动时捕获 `myGate` 实例(工厂作用域变量每轮重赋值,跨轮僵尸子代理读下一轮的门 = 确认语义污染);② **工具超时覆盖**:`AgentTool.timeoutMs` 字段(缺省引擎 60s),`executeToolBatch` 与手动调用路径取 `tool.timeoutMs ?? 60s`——doc_convert(200s)/xxt(310s)声明覆盖,原 60s 统一超时把 login 300s/转换 120s 中途杀掉(CLAUDE.md 声称的"login 300s 超时"此前形同虚设);`AgentTool.execute` 增可选第二参 `ctx?: {signal}`(仅 delegate 消费,其余工具单参兼容)。
  - **引擎 P1**:`AgentEvent.message.usage` 补 `cached`(三方漂移,引擎实际已发);三 provider 历史序列化回归测试(72 用例新增:historyToItems/Messages/Anthropic 输出形状 + 8000 截断 + 角色交替 + detectProvider + trimHistory);导出 estimateMessageTokens/trimHistory/looksLikeCodeLiteral/detectProvider。
  - **渲染端 P0/P1/P2**:`useAgentPanelLayout` hook(Agent 面板高度 JS 动画状态机 + 界面缩放,从 DynamicIsland 逐字搬移,1905→~1760 行);`src/media/imageUtils.ts`(loadImageNaturalSize/sampleImageBrightness,收敛三处「new Image → canvas」);`src/agent/text.ts`(textFromParts/textFromMessage,删 useAgent 死导出 textFromMessage + 统一 4 处内联);`clampExpandedWidth()`(layout.ts,原内联 ×3 收敛)+ 两个 seq effect 合并;WidgetApp `VIEW_WINDOW_H` 键类型 `Partial<Record<PanelView, number>>`(拼错键编译器兜底)。
  - **类型镜像单向引用**(P0,根治双端类型漂移):`src/agent/types.ts` 改为从 `electron/agent/types.ts` **import type re-export + 渲染端扩展**——引擎 types.ts 零 node 运行时依赖(已核实),import type 编译期擦除不打包;渲染端保留 AgentStatus/AgentToolCallState/AgentSession/AgentPanelProps 独有类型,AgentMessage 扩展 usage、AgentConfig 扩展 excludedTools/excludedSkills 必填、AgentToolInfo = Omit<AgentTool,'execute'>;同时给引擎侧 AgentEvent **补 tool-confirm-request**(main.cjs 实际发出但引擎类型缺失)。**修复的实测漂移**:usage.cached、excludedTools/Skills 可选性、tool-confirm-request 缺失。
  - **验证**:72/72 核心测试 + 34/34 markdown + tsc -b + oxlint + 实机 agent 巡检 ×3(含高度/缩放/设置工具/自动回复)+ 音乐展开巡检。
- **核心测试**(`scripts/test-agent-core.mjs`,2026-08-05):后端直测不经 UI —— esbuild 打包测试 bundle(`electron` 别名 stub,Notification 记录到 `global.__notifications` 供断言),mock MCP 服务器(`scripts/test-agent/`):stdio(新行 JSON-RPC,含自杀/慢响应/错误/图像工具)+ sse(GET 事件流 + POST 回传,含直接响应体与 bare 推送变体)。**测试抓到的真实缺陷(均已修)**:① MCP JSON-RPC id 错位——重构 RpcCore 时 requestImpl 递增 nextId 而 send 回调重读 nextId,发送 id 与 pending id 错位,所有请求挂到超时(requestImpl 改为把 id 传给 send);② 技能重名分配不确定——readdir 顺序不保证,同技能集不同运行工具名不同,破坏 LLM 工具记忆与缓存前缀(扫描按目录名字母序排序);③ 记忆并发加载竞态——并发 add 同时触发 load,后完成的 catch 清空刚 push 的条目(10 并发只剩 1 条,loadPromise 互斥)。测试用断言:`node scripts/test-agent-core.mjs`(87 用例:记忆增删改查/去重/上限/串行写/并发互斥/**导入合并 importEntries(去重/置顶/超限淘汰最旧)**、MCP 双传输握手/命名/参数转换/isError/崩溃重启/并发 connect 单进程、skills 扫描/slug/重名/截断/执行、自我配置工具、进化快照/回滚防降级/无 Key 优雅失败、手动调用解析、**设置工具(未注入不注册/hex 归一化/缩放钳制/字体图片文件校验(扩展名/存在/空/超限)/data URL 前缀/列表格式化/名称与 id 校验/play_library_video 透传 id 与空 id 拒绝)**、**get_feature_guide(2026-08-10:注册/无 topic 目录/话题过滤命中/无匹配兜底/纯函数 extractGuideSections 精确命中与截断)**、**extractJsonTitle 严格解析(回归 "['data']"/Python 单引号 dict)**、**sanitizeTitle 前缀剥离/fallbackTitle 首条用户消息派生/sanitizeMind 清洗/总结与心理揣测无 Key 优雅失败**、**raceWithTimeout 超时/中止贯通**、**三 provider 历史序列化形状 + 8000 截断 + 角色交替 + detectProvider + trimHistory**、**settings-store 原子写/防抖合帧/.bak 恢复/加密往返/resetCache**、**tasks 任务注册表(waiting/running 状态块实时可见 + 文案稳定 / 终态触发回调一次 + 失败同样回调 / 回调载荷拼 background-done 标题消息 / 同 id 覆盖 + 移除 + 无任务空串 / pruneTasks 超 TTL 清理)**、**validateRequiredArgs(2026-08-08:空参错误文本含缺失参数名+类型+说明 / 完整参数通过 / 空串视为缺失 / 0 与 false 是合法值 / 无 required 跳过 / _raw 附带原文 / enum 可选值列出)**、**Responses SSE 工具参数累积(2026-08-08 回归:delta 按 output_index 匹配累积 + function_call_arguments.done 权威参数 / 仅 output_item.done 带参数不丢 / maxOutputTokens 覆盖传请求体)**、**set_output_budget(2026-08-08 端到端:引擎初始预算 = 配置 ?? 8192 且越界回退 / action=get 查询不改状态不写配置 / LLM action=set → 预算即时变 + persist 写配置 + 后续请求体带新预算(mock fetch 断言)/ 越界钳制 262144 / 截断响应(response.incomplete)→ 不落定 + 下一轮请求 input 注入预算不足提示(断言 system 消息含 set_output_budget 引导))**)。
- **渲染端**:`src/hooks/useAgent.ts` 订阅事件流,组装消息与状态机(idle/thinking/running/error),流式累积未落定消息、`message` 事件为权威落定,中止丢弃流式消息。**流式性能与竞态优化**(2026-08-06):增量事件(text/reasoning/tool)rAF 合批——跨 IPC 消息的多次 setState 不自动批处理,逐事件渲染 = 每事件全量 Markdown 重解析 + 重测 + 整树 reconcile;事件直写镜像、帧内一次提交,频率压到帧率上限;abort 竞态双修(渲染端 status idle 后迟到的流式事件丢弃,防幽灵文本残留;引擎 abort 同步复位 running + finally 只清自己回合的 controller,防 100ms 内重发被「正在运行中」挡回);已落定消息块(UserBubble/AssistantBlock/ToolSummary/ToolCard)包 React.memo,工具结果一次遍历建 Map 配对(去 O(parts²) 的 parts.find);measureHeight 流式时测量与上报一起按 80ms 节拍(offsetHeight 循环 + getComputedStyle 是强制 reflow,原每帧跑);历史持久化 localStorage(`widget-agent-messages`,**直接同步写不防抖**——防抖 300ms 在页面刷新/渲染进程重启时会丢最后几秒消息)。**历史契约**:send 回传的 history 末尾**即当前轮用户消息**——引擎不再追加(仅历史缺尾时防御性补一条),否则用户消息重复发两遍、且中止后合并的消息会被拆开;上一轮被中止/失败(历史以 user 消息结尾)时,新输入**合并进该未答复的用户消息**(防连续 user 消息污染,LLM 会把未答复请求当"仍待执行"重复执行,见上)。`DynamicIsland` 新增可选 `agent` prop(存在即激活,音乐数据/手势/进度条自动让位):紧凑态 = 四角星图标 + **回复流程监听文案**(thinking+无输出 → 思考中…;thinking+reasoning 流 → 深度思考中…;thinking+文本流 → 正在回复…;running → 正在执行:工具名;idle → 最近回复预览,thinking 复用 loading 点动画);展开 = `AgentView` 聊天面板(用户右气泡强调色、助手左气泡半透明白底、**工具调用按回复收纳成单一汇总列表**(2026-08-06 v2,取代连续序列分组模块:全部工具调用(执行顺序)并入 `.island-agent-tool-summary` 单行,流式执行中也**实时并入**同一列表(useAgent 工具 id 稳定,卡片 open 状态跨事件保留);默认折叠——工具不再夹在文本段之间,再多的工具也不撑开消息气泡,文本段按原顺序独立渲染、汇总行在回复尾部;头部同 v1 的状态汇总(强调色图标 + 名称/「工具调用 ×N」+「● 执行中」/「✓ N」「✕ N」+ 总耗时 + 箭头),点击展开看各卡、再点卡片展开参数;**0fr 折叠必须把 grid item 的 padding-bottom 归零**(Chromium 实测:0fr 轨道残留 item padding 高度,卡片 9px/汇总 8px——收纳态露出被截断的「参数」标题带;padding-bottom 与 grid-template-rows 同节奏过渡 0.28s 无过冲,展开恢复,不额外加 DOM);其余动画同 v1(内层 opacity/translateY 0.2s、箭头 0.28s 弹簧、挂载 island-ui-in;卡片受控状态 + 同款高度动画)、底部输入 Enter 发送(IME 组字不触发)/Shift+Enter 换行、运行中变"停止")。**面板高度自适应**:岛体高度 = `--agent-h` 变量驱动(AgentView 用 scrollHeight 测量内容自然高,clamp [200, 600],消息列表 max-height = `calc(var(--agent-h) - 116px)` 与 AGENT_PANEL_FIXED_H 同源,内容超高滚动不自锁),窗口经 `onAgentPanelHeight` 动态跟随(岛体 + 40,<4px 不 resize),收起回落 280;展开瞬间同帧切 agent 视图,高度目标从第一帧正确(避免"先朝 244 形变再改目标"的二次重定向抖动)。**岛体高度并行动画**(2026-08-06 v2,替代 JS 动画循环:高度走 **CSS 过渡**(与宽度同曲线同时程 cubic-bezier(0.22,1,0.36,1) 0.3s,紧凑 56 → 面板下限 → 测量值全程与宽度同步滑升,不再"先宽后高";React 状态按测量节拍更新(≤12.5Hz),窗口按节拍上报(非逐帧——逐帧 setSize 软件渲染全幅重绘是卡顿根源,窗口透明边缘节拍级跟随不裁剪);骨架期 120ms 让测量提前进形变窗口;旧的"逐事件重定向过渡永不稳定"前提已随 rAF 合批 + 80ms 节流消失;但流式期间仍恢复**高度瞬跳**(`.island-agent-streaming` 去掉高度过渡,80ms 节拍逐格 = 12.5Hz 重绘,远轻于逐帧动画——软件渲染逐帧全幅重绘是"高度展开卡"主因;落定后恢复过渡平滑收尾);**滚动动画挂件版直接跳底**(2026-08-06:软件渲染下即使无模糊,滚动动画逐帧 scrollTop + 全幅重绘仍贵(750×500 文本区 ≈5-15ms/帧),进入面板/发送后 `window.desktop` 时 `scrollTop = scrollHeight` 零动画成本;Web 演示版保留平滑滚动 + 动态高斯模糊(GPU));**Agent 紧凑态悬停不扩展宽度**(2026-08-06 bug 修复:收起面板后鼠标悬浮在岛上,悬停扩展(HOVER_EXTEND_PX 为进度条预留)会让无进度条的 Agent 紧凑岛多出空白占位——`targetPx` 计算按 `agentActiveRef` 排除)。前身 **岛体高度 JS 动画**(2026-08-06 v1:目标值 = 测量结果,显示值经 rAF + easeOutCubic(先快后慢无过冲,高度不宜弹簧过冲)逼近,时长随距离自适应(小步 ~5px 约 0.12s 快跟,大步几十 px 0.3-0.38s 优雅滑行),中途重定向从当前显示值无缝续动;**显示值直接写 DOM 的 --agent-h 变量**(不经 React state,60fps 动画不触发整岛重渲染,React style 不再声明该变量,useLayoutEffect paint 前落起点防 fallback 闪高),每帧上报显示高度给宿主,窗口逐帧跟随(handleAgentPanelSize 高度不设死区、主进程 resize 改 16ms min-interval 节流,原 100ms 防抖会截断逐帧跟随);进入 agent 视图从紧凑高度(ISLAND_COMPACT_H=56)起步滑升,离开复位)。**收起交互**:Agent 模式屏蔽单击岛体/点外/Esc(只保留长按收回 —— 长按消息区空白触发,3D 压感保留;视图内交互区(头部/输入/工具卡片)拦截左键 pointerdown 防误触,右键放行拖拽)。头部右上角为**下拉菜单**(⋯:停止生成(运行中)/新对话(有历史时)/对话历史/工具列表/收起面板,点外自动关闭)。**快捷切换按钮**(2026-08-06):悬浮 ⋯ 时左侧浮现 `.island-agent-quick`——透明命中区自 ⋯ 左缘向左延伸(绝对定位,不参与头部布局,⋯ 不位移),覆盖按钮与 ⋯ 之间的 8px 间隙,鼠标从 ⋯ 横移到按钮的过程悬停不中断、**按钮不消失**(命中区是容器后代,positioned 在容器盒外仍保 :hover);默认显示"收起面板"(末项),滚轮在菜单各入口间逐格切换(每 60px 一步、步间 ≥100ms 冷却、350ms 无滚动重置累积——快速滚动也逐格推进),**每格重挂载按钮重放内容交换动画**(2026-08-06 优化,原"快速上抬后硬停"机械弹跳被用户否决"出戏"):共用 `WheelSwap` 组件(views/WheelSwap.tsx)——旧内容滑出淡出 0.2s + 新内容回弹滑入 0.3s(与 island-ui-in 同款 `cubic-bezier(0.34,1.56,0.64,1)` 轻微过冲),方向随滚轮(向前新内容自下而上/向后自上而下),`.swap` 类仅在 tick>0 后加(首帧挂载不播动画);边框柔和强调 flash(0.3s 缓入缓出,强调色 45% 混底色,不再硬闪),单击执行当前项并复位默认;菜单项与 ⋯ 弹出菜单共用 menuItems 数组(条件项随 busy/messages,索引 ref 跨渲染同步、渲染时钳制有效范围);入场右→左滑入 + 微缩放回弹(0.34,1.56,0.64,1),离场平滑(transition 取目标态,两组曲线分别在隐藏态/显示态写);菜单打开时(.open)隐藏(弹出面板已展开,快捷按钮冗余)。设置视图新增"Agent 设置"入口(`agent-settings` 视图,进 SETTINGS_VIEWS,只能返回键退出;**岛体 540px + 窗口 580(2026-08-06 增高,原 400/440 太扁**:表单/分区内容挤在一起,用户要求);**布局重构(2026-08-07,四组菜单)**——连接(协议提示/API Key/Base URL/模型/思考强度)、行为与界面(自定义提示词/主动陪伴/界面放大——**exec_command 确认门设置已移除**,2026-08-07 用户要求,引擎 confirmExec 字段保留存量兼容)、工具与能力(MCP 服务/技能目录)、记忆与进化(长期记忆/自我进化)、**Sub Agent(总结标题文风 + 心理揣测人格,2026-08-07)**;**数据管理(2026-08-10 用户要求,第 6 组)**:清除数据两段式确认(第一次点击进入确认态 3.5s 自动复位,再次点击执行;`.island-data-clear` 危险色,confirming 实底红)——「清除灵动岛数据」(localStorage.clear + IndexedDB 删 5 库(island-uploads/background/font/audio-library/video-library,blocked 时直接放行——reload 销毁连接后浏览器续删)+ IPC agent:clear-data('app') 删 userData memory.json/memory-state.json/evolution.json/memory-snapshots/settings.json 并重置 memoryStore/evolutionHandle 懒加载单例与 resetSettingsCache(旧缓存会在下次 save 复活))**+ dispose 重建 agentEngine(2026-08-10 修复"LLM 列出记忆 id 但设置视图长期记忆为空"):引擎 tools 在创建时组装一次,内部持有 store 实例引用——只置主进程 memoryStore=null 不够,旧引用继续操作清除前的记忆(LLM list_memory 返回旧 id),渲染端 agent:memory-get 读新实例 → 永久不一致;dispose 清理 MCP 子进程,懒加载下次对话重建**)与「清除工具数据(下载记录及源文件)」(agent:clear-data('tools') 删 userData bili/ 下载与登录态、xxt-profile/xxt-screenshots 登录态与截图;下载中文件被占用抛错 → 返回错误提示稍后重试);执行后提示 + 800ms location.reload 重启界面;preload agentClearData + desktop.d.ts 同步,main.cjs safeHandle 统一 {error}。**整合按钮 + 同行右侧联通展开**(2026-08-07 二次优化,用户要求:悬浮时菜单项**在同一行右侧滑入展开**,与按钮一体联通、非独立气泡浮层——与 ⋯ 快捷按钮同款联通形态,容器后代 + max-width/opacity 过渡 + 逐项错峰滑入;按钮只显示当前菜单名,悬浮展开,单击菜单项切换;按钮上滚轮逐格循环切换——useWheelSteps + WheelSwap 内容动画;**按钮宽度过渡**(2026-08-07 三轮优化:菜单名长度不同,滚轮切换瞬间宽度跳变——React useLayoutEffect 在 paint 前测量新内容宽写 style.width(显式 width 覆盖内容宽),CSS transition: width 0.28s 无过冲从旧显式宽平滑过渡;**测量必须用 scrollWidth**(2026-08-07 四轮修复:切换瞬间按钮宽 = 旧显式宽,新内容被 overflow:hidden 裁剪,getBoundingClientRect 读到裁剪后宽度 → 写回更小值越切越短,实测"只显示一个字多一点";scrollWidth 返回完整内容宽));**选中高亮滑块**(2026-08-07 四轮优化:菜单项 .on 背景瞬移 → 绝对定位 `.island-agent-settings-tabs-indicator` 滑块,React 按 offsetLeft/offsetWidth(布局值,不受滑入 transform 影响)写入,left/width 0.32s 无过冲过渡平滑滑动,菜单项 .on 只提亮文字);**open 一体胶囊视觉**(三轮优化,用户要求"像展开的子菜单":open 时**整个容器变成一个圆角胶囊**(背景/描边 0.22s 过渡),按钮取消独立边框融入胶囊成为第一段,菜单项从右侧滑入,视觉连成一体非独立气泡;菜单项逐项错峰滑入 0.04s 间隔);**抽离通用 QuickMenu 组件(2026-08-07 五轮优化,`views/QuickMenu.tsx`)**:上述整合按钮 + 联通展开 + 滚轮逐格 + 高亮滑块 + 宽度过渡(完整内容测量)全部泛化为 `<QuickMenu<T> {items, value, onChange, getLabel, onSelect?, onExpandChange?, direction?('right'|'left'), wheelWhenOpen?}>`——**四处复用**:① Agent 设置菜单(SETTINGS_TABS,轮换时滚轮切换)② **记忆类型下拉**(MEMORY_TYPES,默认选中**偏好**;输入框宽度随菜单展开 flex 实时收缩——添加行是 flex 容器,input flex:1 + min-width:0 天然联动)③ **Agent ⋯ 菜单**(menuItems,默认选中**新对话**,direction='left' 向左展开,onSelect 执行动作并复位默认,取代原 ⋯ 弹出菜单与悬浮快捷按钮)④ **帮助手册模式按钮**(音乐/Agent,默认**音乐模式**,头部 count 改静态标签);wheelWhenOpen=true 的菜单悬浮展开时滚轮照常切换(设置菜单/⋯/help 是悬浮交互面),记忆类型保持展开时不切(浮层是交互面);**六轮优化(2026-08-07)**:① 记忆类型改 `wheelWhenOpen`(用户实测悬浮展开后滚轮无法切换——统一为展开时也逐格切换)② **子菜单展开动画放慢**(用户要求太快:pop max-width 0.42s / 菜单项滑入 0.36s / 错峰 0.05s 间隔)③ **dir-left 右移 Bug 修复**:flex 布局下 pop 展开让容器向右变宽,按钮(最右端)跟着右移,鼠标悬停位置落在子菜单上(用户实测)——dir-left 的 pop 改 **absolute 定位**(right: calc(100% + 2px),容器宽度 = 按钮宽纹丝不动,pop 向左溢出展开)④ **⋯ 菜单入口固定右上角**(margin-left auto 推最右,status 不定长不挤离右缘)⑤ **buttonAction='run'**(2026-08-07 用户实测:滚轮切换选中后单击快捷按钮无响应——QuickMenu 按钮原只 toggle 展开;⋯ 菜单改单击 = **执行当前选中项**,展开完全由悬浮驱动,执行后复位默认);**七轮优化(2026-08-07,对话 ⋯ 菜单联通背景与动画对齐设置菜单)**:① 展开动画**去整块 transform 平移**(pop 整块滑出的「飞入感」被用户否决)——pop 直接就位在按钮左缘(最终位置),隐藏态 opacity 0 + **visibility hidden**(隐藏态不参与 hit-test:pop 就位后鼠标划过按钮左侧标题区不误触,visibility 延迟 0.26s 到淡出结束才隐藏),展开时菜单项在最终位置**逐个淡入**(与右向 Agent 设置菜单同款观感;「从右向左」进入方向由 item 自身 translateX(14px)→0 提供,用户要求不改进入顺序);② **按钮突出(联通层次感)**:open 时按钮**保持独立浅底+描边**(0.14/0.3,胶囊里的「主入口」),与透明子菜单项形成主从层次——原按钮完全融入胶囊、与菜单项平级无层次(用户实测「快捷按钮没有比子菜单有突出性」);③ 错峰滑入选择器 nth-child → **nth-of-type**(pop 首个子元素是高亮滑块 indicator(span),nth-child 把菜单项错峰整体后移一项——第一项也等 0.05s、末项拖到 0.3s,观感滞后;nth-of-type 按 button 计数精确对应,首项立即滑入;right/left 两方向同受益);**错峰 delay 补齐(2026-08-10 修复"收起为多媒体岛没有入场动画")**:delay 只枚举到 nth-of-type(6),⋯ 菜单最多 8 项(停止生成/新对话/历史/工具/多媒体库/设置/收起为灵动岛/收起为多媒体岛)——第 7、8 项 delay 为 0,展开瞬间提前到位与错峰项脱节(观感=最后两项没有滑入动画);补 nth-of-type(7) 0.3s / (8) 0.35s;**内容区切换动画(三轮优化,专用 keyframes)**——入场 agent-settings-content-in(下移淡入 0.32s 无过冲 cubic-bezier(0.22,1,0.36,1))/离场 agent-settings-content-out(上移淡出 0.2s 缓入),**交叉时序**:离场播 60%(0.12s)即重挂载新内容,入场与离场尾部重叠(旧内容上移淡出中新内容下移淡入,无"慢一拍"停顿感),leaving 期间忽略重复切换;表单状态跨 tab 共享不丢;保存脚/返回键全局;**巡检段 2 逐 tab 断言(含 Sub Agent 预设/自定义),段 1 断言 tab 0 只含连接字段**);**窗口宽度 Bug 修复(2026-08-07)**:WidgetApp `handlePanelViewChange` 对 agent-settings 一律拉回 520——岛体宽度 = 展开宽 × 界面缩放(300% 时 1200px),窗口比岛体窄 → 被裁成"矩形 + UI 没加载全"(实测窗口 522×580)→ 改按 `lastAgentWindowSize` 沿用 Agent 面板当前窗口宽,只调高度(实测 1240×580 完整显示)):API Key / Base URL / 模型 / **自定义提示词**(与长期记忆共同构成系统提示)/ **MCP 服务列表**(stdio/sse 传输切换、启动命令或端点 URL、逐行参数/环境变量/请求头,逐条「测试」连通)/ **技能目录列表**(添加/删除,扫描 SKILL.md,对话中可发 /技能名 直接调用)/ **长期记忆管理器**(类型徽标 + 条目改删、添加行,对话沉淀与人工编辑同库,`agent:memory-get/set`)/ **自我进化区**(「运行记忆进化」按钮 + 进化日志 + 「回滚上次进化」) + **界面放大**(2026-08-08 QuickMenu 化:默认 200,档位 100/150/200/300 + 自定义点击快捷按钮内联输入(100-300 钳制,参考输出预算按钮),localStorage `widget-agent-scale`;原 ScaleStepper 定制步进器已删除;用户明确要求:只放大面板/窗口尺寸,**UI 元素(文字/按钮/气泡)不缩放**——"让程序大一点,眼睛不累"),头部计数与提示行实时显示**当前检测到的协议**(地址含 anthropic → Anthropic Messages)。**界面放大实现**:宽度 = expandedWidth × 缩放(JS,`.island-agent-view` **与 `island-agent-settings-view` 都要 `max-width: none`** 放开基础 500px 上限——否则在设置视图切缩放看不到效果),高度仍由内容驱动(--agent-h,不乘缩放),面板本身不 transform/zoom(UI 不变);窗口经 `onAgentPanelSize`(宽 ×s、高不乘)与 `onAgentPanelWidth`(缩放变化即时跟随,任意 agent 视图都触发)→ IPC `widget:set-size` 跟随,离开 agent 视图由 handlePanelViewChange 恢复 520。Agent 展开态**无 3D 压感**(消息区空白按/长按无操作;紧凑态保留)。**会话历史**:多对话存档(localStorage `widget-agent-sessions`,上限 20 条,`AgentSession {id,title,updatedAt,messages}`);「新对话」= 当前对话(非空)自动存档后清空;下拉菜单「对话历史」→ AgentView 内嵌 history 子视图(标题 = 首条用户消息前 24 字,时间 = 今天 HH:MM/昨天/M月D日,点击加载 = 替换当前对话并从历史移除,行内删除)。**工具列表**:下拉菜单「工具列表」→ tools 子视图(引擎 `listTools()` 暴露名称/描述/参数 schema,经 IPC `agent:tools` → useAgent 加载;卡片可展开参数 JSON)。**预览框随岛体、可滚动**(2026-08-05):tools 视图**不参与高度测量**(`measureHeight` 对 `view === 'tools'` 直接 return)——岛体高度保持进入前的聊天高度,预览列表在剩余空间滚动(`.island-agent-history-list` flex:1 + overflow-y:auto,滚动 CSS 本已具备);刚初始化岛体 200px 时预览框可视高约 135px(≈1.7 个折叠卡片,实测),聊天深入岛体扩展后预览框随之扩展(同一"高度保持"路径,高度不同);返回对话时重新测量。切 tools 前若岛体已被撑大(聊天多),预览框也大——"实时调整"由聊天高度驱动实现。**回复完成自动聚焦**:busy → 空闲过渡时自动聚焦输入框(wasBusyRef 边沿检测,chat 视图内),连续对话直接输入。**进入对话面板自动滚动到底**(2026-08-06):chat 视图内容挂载后(phase content)滚动到最近信息——恢复历史/展开面板时用户期望看到最新消息,不停留在旧位置。**新对话无历史消息时不模糊**(2026-08-06):smoothScrollTo 增 `blur` 参数,进入面板的滚动按 `messages.length > 0` 决定是否启用动态高斯模糊(模糊是长消息列表滚动动画的性能优化,新对话没内容可优化;messages 长度经 ref 读取不入 effect 依赖——消息到达时发送路径已平滑滚动,这里只在视图/骨架切换时滚一次)。**动画性能(易踩坑)**:① 流式回复中高度**不用 CSS 过渡**(改由 JS 动画循环驱动,见面板高度自适应段;原 `.island-agent-streaming` 类与 CSS 高度过渡已删除——每帧启动 CSS 过渡 = 过渡永不稳定 + 每帧布局重排,是"加载文字卡"主因;配合 AgentView 测量 80ms trailing 节流(streaming 时)与 rAF 合批,高度 60fps 平滑跟追目标);② agent 面板进入动画改纯淡入(maskPanelIn)——大面板首帧挂载 + scale 动画在软件渲染下逐帧重光栅化;③ 收起为**单动画**(与音乐模式一致:宽度/高度同时收缩 + 压感回弹同 tick,doCollapse 统一执行)——两阶段(先缩高再变胶囊)被用户否决(割裂、中间卡顿);④ **Agent 模式禁用长按收回**(长按展开态无操作,只保留按压反馈;收起唯一入口 = 右上角 ⋯ 菜单的"收起面板")。**输入框贴底**:消息列表 `flex: 1 1 auto` 拉伸填满剩余空间(内容不足时输入框贴底、欢迎语 margin auto 居中),测量 scrollHeight 在内容不足时 = 可视高(自收敛),超高时 = 内容全高,自适应稳定。**展开动画**:agent 视图宽度过渡用**无过冲缓动 0.3s**(紧凑 100px → 展开 400px 大跨度弹簧过冲在软件渲染下抖动明显,用户反馈卡顿);AgentView 测量 rAF 延迟一帧(首帧挂载不阻塞展开布局);**骨架屏两阶段**(AGENT_PHASE_IN_MS=120):展开首帧渲染轻量骨架占位(3 条脉冲灰条,头部/输入框照常,骨架期不测量保持岛体下限),延迟后挂载真实消息内容淡入并测量长高——形变动画期间 DOM 极小,展开更顺。Agent 模式主题色 = 自定义色 ?? `#4d6bfe`。**紧凑态文字 emoji**:`truncateText` 按字素截断(Intl.Segmenter,退 code point),UTF-16 按代码单元切片会劈开 emoji 代理对显示 �;字体栈补 'Segoe UI Emoji'(widget.css 与 index.css 的 body)。
- **消息气泡 Markdown 渲染**(2026-08-06):用户/助手/流式文本改走手写零依赖渲染器——**解析器 `views/markdownParser.ts`(纯 TS,不依赖 DOM/React,可 node 直测)+ 组件 `views/Markdown.tsx`**。GFM 子集:段落(单换行 = 软换行,双换行 = 新段落)、标题/setext、hr、列表(缩进嵌套、有序 start 序号)、引用(递归,可嵌列表/代码块)、围栏代码块(```lang,头部语言标签 + 复制按钮)、**GFM 表格**(对齐 `:---:`、`\|` 转义、行内代码内的管道不拆分)、行内 **粗体/斜体(下划线两侧贴词字符不强调,防标识符 foo_bar 误伤)/删除线/行内代码/链接/裸 URL 自动链接(去尾随标点)**。**对话媒体窗口(2026-08-08)**:`![alt](url)` 按扩展名分派(`mediaKindOf`:data:image/ 前缀 / .png .jpg .gif .webp .bmp → img、.mp4 .m4v .mov .webm → video、.mp3 .wav .flac .ogg .oga .opus .m4a .aac → audio,无媒体扩展名转链接)→ `MediaFrame` 组件:远程 https/data:/blob: 直接加载(CSP img/media 已放行 https:),**file:/本地路径映射 `island-media://local/<编码路径>` 自定义协议流式播放**(2026-08-08 二次改造,替代 IPC 全量 Buffer:200MB 上限全量读取 + IPC 克隆双倍占内存,10GB 视频直接 OOM——协议 `fs.createReadStream` 转 Web ReadableStream 分块发送,内存 ≈ 块大小;**完整 Range 支持**(206 + Content-Range,视频 seek 必需——不用 net.fetch(file://),Chromium file:// 请求不支持 Range 返回 416 实测);扩展名校验(仅媒体可访问防任意文件读取)+ **按类型大小上限:视频 10GB / 音频 1GB / 图片 10GB**(2026-08-08 用户要求,超限 413,渲染端 error 提示);协议特权 registerSchemesAsPrivileged 在 app ready 前(stream/standard/secure/supportFetchAPI),handler ready 后挂载;CSP img/media-src 加 island-media:);**气泡 UI 包裹 + 四周 8 手柄拖拽等比例缩放(2026-08-08 用户要求,替代原右下角单手柄)**:图片/视频 = 圆角卡片气泡(描边 + 阴影,悬浮强调色光环),8 个手柄(4 边条透明命中区 + 4 角点圆点,光标按方向)拖拽投影到宽度变化(锚点 = 对角,w′ = w + sx·dx + sy·dy×aspect,钳制 120-640,`HANDLE_SIGN` 表);**音频 = QQ/微信风格语音气泡**(`VoiceBubble`::胶囊横条 + 圆形播放键(播放中变暂停)+ 5 条动态声波(播放时逐条跳动 voice-wave keyframes)+ 进度细条 + 时长(秒/分′秒),点击整条切换播放/暂停,不参与拖拽缩放;用户气泡内深色系适配);**定制视频播放器 VideoPlayer(2026-08-08 用户要求"不要原生控件")**:自定义控件层(底部渐变遮罩 + 圆形播放键 + 可拖动 seek 进度条 + 时间 + 全屏按钮);全屏 = **整个播放器容器 requestFullscreen**(控件随容器进入全屏层,原生 video 全屏层带不了自定义控件);**全屏进入/退出动画**(media-fs-in 缩放淡入 + ::backdrop 淡入 / 退出 leaving-fullscreen 缩回淡出,fullscreenchange 检测);**全屏拖拽(2026-08-08 三轮修复:用户要求"全屏时右键拖拽移动窗口,不退出全屏、不越来越大")**:全屏层 = 100% viewport,窗口任何 setWindowSize 都会让全屏层跟随 resize 放大 = "越来越大"的根因——WidgetApp 加 `setWinSize` 统一出口(全屏期间跳过尺寸变更,移动窗口的 setPosition 不受影响,全屏层跟随窗口移动是标准行为;fullscreenRef 监听 fullscreenchange);handleDragPointerDown 全屏时**正常拖拽**(不再 exitFullscreen);**右下角单手柄(2026-08-08 用户要求"四周改回右下角触发")**:简约 grip 图标(斜线箭头 + 圆底,悬浮浮现),等比例缩放 dw = dx + dy×aspect;原 8 手柄/HANDLE_SIGN 已删;**拖拽岛体底部自动跟随(2026-08-08 用户要求"拖多大自动往下滚多少")**:followMediaInView 在 **useEffect([width])** 里执行(React 提交后布局已更新——pointermove 时 setWidth 异步,旧布局读不到新高度,实测 scrollFollowed 失败后改为 effect 触发成功)——媒体底部超出消息列表可视区即滚动对齐,拖小回滚顶部。**aspect 未知 16/9 兜底(2026-08-08 修复"播放视频看不到视频")**:video 元素没有内在尺寸,容器 aspectRatio 未设置时高度 0 = 视频不可见——aspect 加载前用 `16 / 9` 兜底,元数据到达后按真实比例修正;**CSP media-src 补 `data:`**(data URL 视频/音频原被 CSP 拦截 → onError 显示"无法播放"而非 video,实测);巡检模式 `WIDGET_SCREENSHOT_MODE=chat-media`(对话媒体 UI 巡检:切 Agent 模式 → 展开 → MediaRecorder 录真实 webm(data URL)→ 注入 agent:event message(media part,与 open_file 媒体附件同链路)→ 断言消息气泡 + video 元素 + 可见高度 + 截图,结束恢复消息并切回音乐模式)。**播放失败按错误码区分文案 + 降级打开**(2026-08-08 用户"显示无法播放该文件":本地协议全链路已实测通过(chat-media 巡检:island-media:// 本地 webm readyState 4 + decodeError null)——"无法播放"= 视频格式 Chromium 无法解码(硬限制:窗口内只支持 H.264 mp4 / webm(vp8-vp9)/ ogg,HEVC/H.265、mkv、avi、flv 不支持);MediaFrame onError 记录 video.error.code,code 4(SRC_NOT_SUPPORTED)文案明确"该视频格式无法在窗口内播放(窗口内支持 mp4(H.264)/webm/ogg)",其余"无法播放该文件(可能已移动、过大或格式不支持)";`MediaError` 按钮 → 新 IPC `app:open-media-external`(preload `openMediaExternal`):远程 URL 走 shell.openExternal,本地路径解码后扩展名同协议校验再 shell.openPath——外部播放器仅为降级选择,正常播放全在窗口内);**媒体附件自动播放(2026-08-10 用户要求三轮)**:LLM 播放视频/音频 → 媒体元素加载出后自动播放——VoiceBubble/VideoPlayer/MediaFrame 新增 `autoPlay` prop(媒体就绪 canplay 后 play,晚于 loadeddata 封面抓帧不冲突;被自动播放策略拦截静默回退封面/播放键,不误报错误;面板重挂载场景先 seek 缓存位置再播续上);AssistantBlock 只自动播每条消息**第一条**媒体附件(多条不全响,其余点一下播放);**只限"当次对话"(三轮用户要求:历史/重挂载不再自动播)**——useAgent 维护 `mediaAutoPlayRef` Set(本会话流式落定且未消费的消息 id;message 事件落定 add,clear/loadSession 清空),AgentView 传 AssistantBlock `mediaAutoPlay={Set.has(m.id)}`,渲染后 effect 调 `consumeMediaAutoPlay` 消费(重挂载渲染读到 false);AgentPanelProps/AgentController 补 mediaAutoPlayIds/onMediaAutoPlayed;markdown ![url] 内嵌媒体保持被动(默认 false)。**四轮修复(2026-08-10 用户实测"找歌来听没自动播放"+ 严格测试 chat-media 巡检扩断言)**:① **markdown 内嵌媒体也自动播**——LLM 常用回复内嵌 ![歌名](路径) 而非工具拦截的 media part:`firstMediaKindInText`(markdownParser 导出,MD_IMAGE_RE 取第一个媒体链接的 mediaKindOf)分派——AssistantBlock 按原 parts 顺序找**第一个**音频/视频媒体(文本段 markdown 内嵌或 media part 谁先谁获权),文本段传 Markdown 组件 `mediaAutoPlay` prop(模块级 `mdAutoPlayRemaining` 消费:该组件内第一个音频/视频用权,图片不占权;渲染同步单线程 + StrictMode 二次赋值安全);② **readyState >= 1 即 play**(原等 canplay:preload=metadata 时浏览器不预载数据 canplay 可能**永不触发**,短媒体播完/未播静默失败实测);③ **挂载时一次性捕获 autoPlay(useRef,依赖 [] 不重跑)**——消费标记使 prop 变 false 后,若 effect 依赖 [autoPlay] 会重跑:cleanup 移除 loadedmetadata 监听,加载慢的媒体(本地协议流式)永远等不到 play()(音频实测根因,视频因无后续重渲染侥幸成功);重挂载新实例读 false 不播语义不变;④ main.cjs 显式 `autoplay-policy: no-user-gesture-required`(Electron 页面脚本 play() 本就放行,显式声明防版本变化)。巡检 chat-media 扩 4.10/4.11 段:注入消息断言每条第一个媒体自动播放(played = !paused || ended——0.9s webm 播完 ended 同样证明)、桥 getConversationMedia 端到端(视频带播放状态/音量/速度/循环/全屏);开头清 localStorage + reload 防上次残留污染;断言须在 4.6 音频移交(切音乐模式面板卸载)与 4.8(reload)之前;渲染进程 console 不转发主进程,诊断须写 window 全局对象(巡检 executeJavaScript 读取)。初始宽 = 媒体窗口默认设置(localStorage `widget-media-window`,键/读取定义在 settingsBridge `readMediaWindowWidth` 单一来源,钳 160-800 缺省 320)。**设置入口**:Agent 设置「行为与界面」组新增「媒体窗口默认宽」(QuickMenu 档位 240/320/480/640 + allowCustom 输入,即时生效);**LLM 工具 `set_media_window_size`**(settingsTools,width 钳制 160-800,桥 `setMediaWindowSize` 返回 previous 原值,已是目标值提示「无需修改」)。**多媒体库(2026-08-08)**:`media-library` 面板视图——**独立菜单**(2026-08-08 用户要求:不属设置范畴,设置视图入口移除,返回键 = 从哪来回哪去——托盘呼出收起岛体、Agent ⋯ 菜单呼出回对话视图,`mediaLibraryBackRef` 记录);**库切换复用 QuickMenu**(与 Agent 设置左上角菜单同款:整合按钮 + 滚轮逐格循环切换 + 高亮滑块,`.island-media-lib-menu`);**Agent 设置面板大小**(岛体 540 + 窗口 580,类 `island-lib-view island-media-lib-view` 覆盖 440,`VIEW_WINDOW_H['media-library']` 登记——**此前未登记窗口停在 280,岛体底部被窗口裁切 = "UI 底部截断" bug**)。**图片/音频/视频三 tab**:图片复用 island-background 库(缩略图网格/改名/删除/上传;**右键菜单「应用到展开态/紧凑态背景」**(2026-08-08:点击直接导入对应形态槽位(裁切参数复位默认)+ 跳转背景编辑器,省略导入步骤,只需手动调整方位/参数;宿主 `onApplyImageToBackground` 组装背景参数并切视图(**返回目标 = 多媒体库**:`backgroundBackRef` 记录,背景编辑器返回键直接回多媒体库而非设置视图,2026-08-08 用户要求;巡检断言 backToMediaLib);菜单**点外关闭**(menuRef contains 判定——原 document pointerdown 先清 ctxMenu,按钮 click 读不到状态,应用不生效实测);**菜单 absolute 定位在面板内 + 相对坐标(2026-08-08 二轮修复"初次进入还是无法右键")**:原 fixed 定位在岛体带 transform(悬停上浮 translateY)时包含块变成岛体,视口坐标被当相对岛体坐标 → 菜单错位到岛体边缘被 overflow:hidden 裁剪 = 右键"没反应"(元素存在但不可见,巡检 menu:true 是假阳性——合成事件不经命中测试,按钮点得到);`.island-panel-list` 补 position:relative,坐标 = clientX - 面板 rect,菜单随岛体移动永不被裁剪;巡检加 menuVisible 断言(菜单 rect 必须在面板可视范围内));WidgetApp 补 `mediaLibrary` scope 监听(LLM 导入音频/视频后列表即时重读)));**音频库**(IndexedDB `island-audio-library`,条目 ArrayBuffer,上限 200MB:列表 + 行内试听(**定制播放条** AudioPlayBar——圆形播放键 + 可拖动 seek 进度条 + 时间(**UI 参考音乐模式进度条:4px 圆角条 + 强调色渐变填充,悬停/拖拽显示圆点 thumb**);点击 ▶ **挂载即自动播放**(autoPlay,2026-08-08 用户要求;视频同款 autoPlay);**blob URL 按条目缓存**(原每次渲染重建 URL,父组件任何 state 变化都换 src 中断播放 = "点击播放无响应"根因之一,条目移除时 revoke)——圆形播放键 + 可拖动 seek 进度条 + 时间,替代原生 audio controls,2026-08-08 用户要求"播放条太丑")+ 单个/批量导入播放列表 + 改名/删除/导入;**导入 type 用 `inferAudioType(name, f.type)` 扩展名兜底**——File.type 常为空,空 type 的 Blob 音频无法播放 = 导入音频无法播放 Bug 根因,2026-08-08 修复);**视频库**(IndexedDB `island-video-library`,**路径引用**——视频 GB 级不入库,导入经主进程 `app:pick-media-files` 对话框选文件记路径,岛内 `<video>` 经 island-media:// 协议流式播放 + 改名/删除)。**卡片 UI 统一(2026-08-08)**:行内操作按钮全部换简约 SVG 图标(播放/暂停三角、列表+导入、铅笔编辑、垃圾桶删除,替代原 ▶/➕/✎/✕ 文本符号);编辑进入 = 输入框 island-ui-enter 回弹淡入(**根因修复:`.island-ui-enter` CSS 规则此前从未定义**——只有引用,编辑/记忆/技能等所有入场动画都没播;base.css 补定义后全局恢复),保存完成 = 行尾绿色 ✓ 徽标回弹浮出(600ms);试听/播放展开/收起 = grid-rows 0fr↔1fr 高度过渡 + 内容淡入——**收起延迟卸载(2026-08-08 修复"收起动画没有做")**:closingId 期间保留内容播完 0.32s 过渡再卸载(原收起瞬间内容卸载,过渡无内容可缩 = 高度瞬变);**收起完善(2026-08-08 二轮)**:收起瞬间暂停播放(行 data-preview-id 定位 video/audio pause——声音立即停,画面保留播收起动画,声音继续+画面被压扁是"生硬"主因)+ inner 上移淡出过渡(not(.open) 时 opacity/translateY 0.22s,与展开 island-ui-in 对称);**音频导入播放列表 → 自动切回音乐模式播放**(2026-08-08 用户要求:handleAddLibraryTracks 调 addLibraryTracks(自动播首曲)+ setMode(music),模式切换动画自动收起岛体——停在多媒体库面板"导入成功却没反应"很奇怪);**播放键丢失修复(2026-08-10 用户实测"多媒体库播放视频,进度条左侧播放键默认丢失,点击后才一瞬间显示")**:三处根因——① `.island-media-lib-play` 缺 `position: relative`(PlayPauseSwitch 图标 absolute inset:0 定位到最近 positioned 祖先 = 播放器容器左上角被 video 盖住,按钮里看不到图标;与 `.island-media-playbar-btn` 同款补上);② 该按钮没挂 `is-playing` 类(图标态恒为暂停态);③ **PlayPauseSwitch 图标语义写反了(与注释矛盾)**:原 CSS 默认隐藏三角/显示双杠——暂停状态显示"暂停图标"、播放中显示"播放三角";修正为默认三角(播放键)显示、is-playing 时双杠显示(旋转交叉动画保留,语义与注释一致)。**勾选框定制**(原生 checkbox 无动画:input 隐藏,视觉框 + 对勾 SVG 描线动画(stroke-dashoffset 勾选描出/取消描回)+ 背景填充 + 微放大;**批量导入播放列表按钮亮起/熄灭**(勾选后 :not(:disabled) 强调色底 + 呼吸光 island-batch-glow 动画,取消熄灭;巡检断言 on/offset/btnGlow);列表宽度与搜索栏对齐(原 14px 内边距使行窄于搜索栏)。**播放列表 ↔ 音频库同步**(useMediaPlayer.addTracks):上传歌曲到播放列表时**音频库无同名(按文件名)则自动补录**,已有不重复导入(参考图片库导入机制);`addLibraryTracks` 从音频库导入播放列表(单个/批量,存 island-uploads 持久化,自动播放首曲)。**呼出**:托盘菜单「多媒体库…」(`widget:open-media-library` → preload onOpenMediaLibrary → WidgetApp seq → DynamicIsland requestMediaLibrarySeq 展开切视图)+ **Agent 对话 ⋯ 菜单「多媒体库」**(AgentView menuItems,onOpenMediaLibrary → 切 media-library 视图)。**LLM 工具 9 个**(settingsTools,桥操作 IndexedDB):list/import/rename/remove_audio_library(import 读文件 → data URL ≤200MB 经桥解码 ArrayBuffer)+ list/import/rename/remove_video_library(import 校验扩展名/存在/≤10GB 记路径)+ **play_library_video**(跳转多媒体库播放指定视频,2026-08-10,见工具系统段);**删除库条目不影响播放列表**(播放列表独立存储 island-uploads)。**流式友好**:未闭合的围栏/表格/强调在增量文本里退化为普通段落,补齐即成形,每次增量重解析天然收敛。**安全**:全部文本经 React 转义输出,无 HTML 注入面(无 dangerouslySetInnerHTML,唯一例外是 mermaid SVG——其 securityLevel 'strict' 自带转义)。**Mermaid 图表**:```mermaid 围栏 → `MermaidBlock` 懒加载 mermaid(dynamic import 构建分包,主包不含;模块级单例 + **按代码字符串的 SVG 缓存**——流式重解析/视图切换同代码直接复用不重复渲染,失败代码缓存不再重试,失败回退源码代码块 + 提示);初始化读岛体实时计算样式(--state-color 强调色 / fontFamily),深色主题匹配挂件;SVG 宽高自适应(max-width 100%,宽图横向滚动)。**用户气泡 plainMermaid**:mermaid 按普通代码块显示(图表深色主题塞进浅色强调底不可读)。**链接**:仅 http(s) 渲染为锚点,点击经 `window.desktop.openExternal`(preload 新增,main.cjs `app:open-external` 处理器校验 http/https 后 `shell.openExternal`,Web 演示版回退 window.open)。**CopyButton 从 AgentView 迁到 Markdown.tsx**(消息脚注与代码块头部共用)。**易踩坑(实测抓出)**:共享 /g 行内正则单例 + `exec` 循环 + 递归 parseInlines —— 内层递归把 lastIndex 重置为 0,外层从 0 重扫同一匹配 = **死循环 OOM**(`~~s~~` 链接分支复现,堆 4GB 被打爆);必须用 `text.matchAll`(内部克隆正则,递归安全);另:表格类型 = `header: MdInline[][] / rows: MdInline[][][]`(行 = 格数组,格 = 行内节点数组),`string[].map(parseInlines)` 返回二维数组不是一维。测试:`pnpm test:markdown`(39 断言,含递归不循环回归)。

## 关键约束(容易踩坑)

- **提示文本必须渲染在灵动岛体内部,紧凑态与展开态均不允许超出岛体轮廓**(用户明确要求)。实现:`DynamicIsland` 的 `hint` prop(纯文本)——紧凑态直接注入左侧文字区(与歌名同款字体、同套切换动画),展开态渲染在播放键下方(`island-hint-play`,面板次级文字同款、无气泡)。**禁止**气泡式 Toast UI,也禁止渲染到岛体外(如窗口底部)的提示。
- **透明窗口渲染稳定性**:widget/widget.css 去掉岛体毛玻璃(backdrop-filter 在透明窗口合成不稳)与逐帧 blur(卡顿主因);岛体背景全不透明(rgb(8,10,14));展开面板高度 244px(覆盖组件默认 208px;控制区 flex: none 贴进度条,无居中留白);歌词在挂件版改回文档流(static),歌词行 42px 高(两行 0.78rem×1.4 歌词 + 底部余量,防 j/g/y 下沿裁切)。**垂直居中用 `transform: translateY(-50%)`,不要用 `translate` 属性**(透明窗口下偶发失效导致元素整体偏下,`.island-extra`/`.island-time-particles` 都为此改过)。
- **歌词折叠**:折叠条件 `lyricFold = !lyricShown || (歌词查询完成且无结果)`(查询中保持展开防闪动)。折叠时岛体加 `island-lyric-off` 类,挂件版高度 244→202px;动画为**方向性过渡**——展开用回弹曲线(scaleY 过冲,`cubic-bezier(0.34,1.56,0.64,1)`),收起用无过冲缓动(scaleY 过冲会翻转为负值镜像)。挂件版静态定位,折叠 transform 是纯 `scaleY(0)`(不能带基础版的 translateX(-50%) 居中位移);基础版歌词是绝对定位,只淡出不收高度。
- **歌词查询竞态(useLyrics)**:本地播放器 idle 时 `player.track` 仍指向列表首曲(index 默认 0);外部监听短暂回落本地(externalActive 瞬时为 false)会误用本地首曲发起歌词查询。useLyrics 的响应在应用前按 `lastKeyRef` 校验,过期响应(曲目已切换)一律丢弃,否则旧响应会覆盖新曲目歌词且不会重查。
- 主进程已 `app.disableHardwareAcceleration()
// HEVC(H.265)硬解(2026-08-08,用户下载视频"无法播放该文件"):Chromium 默认不支持 HEVC;系统装有「HEVC 视频扩展」(Win11 常见)时经 Media Foundation 硬解,对话窗口内即可播放 HEVC mp4——无扩展时此开关静默无效(仍走格式提示 + 系统播放器降级)。**真实文件 UI 验证(JiJiDown 下载目录)**:chat-media 巡检优先扫描 `C:\Program Files\JiJiDown\Download` 第一个 mp4/mp3 注入——真实 mp4(中文名+空格路径)与 mp3 均 readyState 4 + decodeError null(本地协议完整解码,mp3 时长 3'55″ 解析成功),目录不可读回退 MediaRecorder webm`(避免半透明 alpha 突变),改动渲染相关代码不要重新引入硬件加速依赖。
- 组件内宽度/布局靠 JS 测量 + px→px 过渡(弹簧曲线),文字切换/悬停伸缩/展开收起的时序常量集中在 DynamicIsland.tsx 顶部,改动画同步时两边(CSS transition 与 JS setTimeout)要一致。
- 已知平台限制:QQ音乐等客户端可能不支持 SMTC 跳转/模式控制,点击后 1.2s 检测未生效 → 岛内提示并回退(UI 显示系统真实状态)。
- **SMTC 位置平滑(useSystemMedia)**:浏览器(Chrome/Edge 标签页音频)等平台的上报位置不可信(抖动或**阶梯式过期**——冻结数秒后突然跳变,跟随会抽搐或周期性回跳)。显示进度 = 锚定基准 + 本地时钟流逝,常规轮询不跟随上报位置,仅重锚定于:① 曲目变化(标题 key);② 播放状态变化(暂停冻结在本地时钟位置、恢复继续,均不采信可能过期的上报);③ 位置偏离 > 5s **且**上报位置距上次轮询移动 ≥ 2s("活着"判定,真实外部 seek 才满足;浏览器冻结期移动≈0、更新瞬间偏差≈0,永远不满足)。**恢复播放必须重置流逝基准**(`baseAtRef = now`):暂停冻结后 baseAt 停在暂停时刻,恢复时 `position = base + (now - baseAt)/1000` 会把**暂停时长算进去**,恢复瞬间时间突跳、显示比进度条长(用户实测反馈)——轮询 `playChanged && smPlaying` 分支与 `control('play')` 都要更新;乐观切歌(next/previous)base 归零。用户 seek 在 `control()` 内立即乐观锚定,是否生效由**挂起验证**判定(对照系统真实位置,±3s 或单次跳变 >5s 视为生效——后者覆盖浏览器阶梯式更新,其位置块状前进可能永远不在目标 ±3s 内):客户端明确拒绝(`accepted === false`)或 **3s 超时**(2026-08-06,原 8s 用户实测感觉太慢才判定"不支持拖拽")未跟随(QQ音乐"接受但未跳转")→ 回退显示、判定该平台不支持 seek 并返回 `false`,宿主据此提示。**验证结果按 sourceAppId 持久化**(localStorage `island-seek-support`),切换平台/重启都不重学,已记忆的平台零等待直接拒绝/放行。暂停点击瞬间由 control() 冻结位置。**歌词定位单独成轨**(2026-08-06,`lyricPosition`,与 position 分离):显示进度的 5s 偏离阈值对原生客户端(每秒 +1s)永达不到,锚定后整曲自由漂移——歌词与实际播放偶尔快偶尔慢的根因。lyricPosition 每轮询重算:**上报"活着"(移动 ≥ 0.5s,原生客户端每秒 +1s)直接采信上报**(1s 粒度对歌词行切换足够,杜绝漂移);上报冻结(浏览器阶梯式过期:数秒不移动)本地时钟插值兜底(平台实际在播);暂停停最后位置、流逝基准更新到暂停时刻(恢复后若上报未立即解冻,插值从暂停时刻继续,不把暂停时长计入)。**歌词渲染用 lyricPosition**(App/WidgetApp 的 useLyrics 第三参),进度条仍用 position(平滑优先);本地播放器路径不受影响(player.position 本就准确)。
- **歌词 API 接入点**(2026-08-06):预设厂家(**QQ音乐默认** / 网易云音乐 / 酷我音乐 / 酷狗音乐 / 自定义 URL 模板)+ 设置视图新增「歌词 API」入口(`lyric-api` 视图,进 SETTINGS_VIEWS)——`src/media/lyricProviders.ts`(localStorage `widget-lyric-provider`),useLyrics 请求带 `provider` 参数(自定义带 `url` 模板,{title}/{artist} 占位),桥接按厂商实现:QQ音乐(client_search_cp 搜索 songmid + fcg_query_lyric_new,**歌词字段 base64 解码**)/ 网易云(search/get/web + song/lyric)/ **酷狗音乐**(songsearch_v2 搜索 FileHash + m.kugou.com/app/i/krc.php?cmd=100 明文 LRC)/ **酷我音乐**(search.kuwo.cn 返回**单引号非标准 JSON**,宽松替换后解析 + pickBest;歌词接口 m.kuwo.cn 已封闭 → **fallback 到 QQ 歌词兜底**)/ 自定义(LRC 文本或 {"lrc"} JSON)。**按监听平台自动切换**(2026-08-06):`PLATFORM_LYRIC_MAP`(qqmusic→QQ / netease→网易云 / kugou→酷狗 / kuwo→酷我),useLyrics 的 `platformId` 参数(WidgetApp 传 system.platform.id)——浏览器/汽水等无公开歌词 API 的平台不映射,回退手动配置;本地播放同理。**自动切换开关**(2026-08-06):设置视图歌词 API 页新增开关(`widget-lyric-auto`,localStorage,**默认开启**)——开启时按监听平台自动换对应厂商;关闭后一直按手动选择生效;`resolveLyricProvider` 内读开关判定。开关 UI 为定制 toggle(轨道 + 滑块,**滑块回弹滑动 0.3s cubic-bezier(0.34,1.56,0.64,1) + 轨道颜色过渡**)。**切换即刷新**(2026-08-06):查询 key 含 provider(id|title|artist)+ effect 依赖 platformId——原实现 key 只有 title|artist,切厂商不触发查询,需重播才有新歌词(实测);桥接缓存 key 本就含 provider。**搜索匹配增强**(2026-08-06):`pickBestHit` 相似度评分(标题精确 100/包含 60/前缀 20 + 歌手互相包含 30,搜索 10-20 条)——无脑取首条在歌名短/带副标题/歌手缺失时会对不上(实测 QQ 监听歌词完全错);网易云/酷狗/酷我共用。**播放键下方提示显示对应厂商名**(ControlView,歌词开关打开时)。**设置面板与歌词 API 视图岛体大面板高度**(2026-08-07 用户要求增高):settings 视图六个入口(背景/帮助/主题色/字体/Agent 设置/歌词 API)在基础 244px 下挤在一起需滚动,增高到 **440px**(与背景编辑器/库页面同款大面板),lyric-api 视图高度对齐同一值——根类名 `island-settings-view`/`island-lyric-api-view`(DynamicIsland 拼接),CSS `height: 440px`,挂件窗口 480 复用 `BG_VIEW_WINDOW_H`(VIEW_WINDOW_H 登记 settings/lyric-api,无新增常量);高度过渡继承基础 `.island-demo.expanded` 弹簧过渡。
- **岛体悬停校准(DynamicIsland 布局 effect)**:点击穿透窗口下 mouseleave 偶发丢失,JS 记录的悬停态(hoveredRef)会滞留,导致"宽岛无进度条"(宽度被设成悬停扩展宽,但进度条可见性由 CSS :hover 驱动)。布局 effect 每次重算宽度前用 `island.matches(':hover')` 校准,悬停态滞留就回落自然宽。

## 双岛并存模式(设计文档,未实现 —— 2026-08-07 四路审计核实:代码零匹配)

> ⚠️ **本段是设计文档,尚未落地**:全仓库(排除 node_modules/.git/release)对 `dual` 零匹配——main.cjs 的 `widget:set-mode` 只接受 music/agent、托盘 radio 只有两项、无 dual-shot.cjs、git 历史无相关提交。后续开发者请勿按"已落地"假设推进;实现时按 screenshot-tests.cjs 先例把组合状态机独立成 `electron/dual.cjs`(main.cjs 已 1180 行)。

托盘"模式"新增第三项 **双岛并存模式**(settings.json `mode: 'dual'`,radio 三选一):音乐灵动岛与 Agent 灵动岛**各占一个窗口并存**(`widget.html?island=music|agent` 查询参数注入角色,权威值经 IPC `dual:get-role`/`widget:set-mode` payload 的 role 下发;单窗口升级为音乐窗口时无需重载页面)。两岛独立拖拽、独立展开,支持**拖拽组合**与**展开态模式切换条**。

### 组合状态机(主进程 = 布局权威)

- **堆叠**(紧凑+紧凑):拖拽松手距离 < 100px → 音乐行锚定(窗口收至岛体尺寸、岛体位置不变),Agent 行补间(320ms easeInOutCubic)到音乐下方;单元宽度 = 音乐岛宽度,**Agent 岛拉伸到同宽**(`.dual-stretch`,内容居中、两侧背景填充),分割线(渐变强调色条)在两窗相邻处合成,朝向边缘 16px 热区悬停提示。音乐岛文字变宽实时重排单元。**右键长按分割线热区 = 脱离**(400ms 后先解除组合再继续拖拽本窗口;脱离 800ms 冷却防"刚脱离又吸回")。
- **停靠**(紧凑+展开):紧凑岛拖近展开岛(≤150px 开始收缩成 LOGO 球,≤90px 松手)→ 主进程补间窗口(380ms)收缩到 34×34 球位(展开岛切换条插槽中心)→ 隐藏窗口(合并进切换条,对方 LOGO 呼吸点亮)。**右键长按切换条未移动 = 解除停靠**(球从插槽弹出,紧凑岛落回插槽方向的岛体旁)。展开岛收起时停靠自动解除。
- **黑洞吸纳**(展开+展开):拖近(≤130px)松手 → 被拖岛补间(460ms)收缩飞向目标岛插槽,渲染端同步播放事件视界 + 粒子撕裂 + 岛体扭曲(skew/blur 沿拉扯方向)+ 淡出 → 收缩成球合并进目标岛切换条。
- **切换主体岛**(切换条点击/滚轮):本岛收起(渲染端动画),目标窗口移到主体岛右侧展开;停靠的目标先恢复。双窗口可同时展开(各自有切换条),但模式切换保证"一主体岛"语义。

### 模式切换条(展开态;渲染端物理 + 插槽上报)

- 展开即从岛体**分裂钻出**(scale 0.25→1.12→0.96 回弹 0.55s)吸附在**左上角**(默认插槽,竖条 28×48),两个 LOGO(音乐音符/四角星)当前模式高亮。
- **滚轮切换**(useWheelSteps 逐格手感)/ **单击 LOGO 切换**;对方停靠时其 LOGO 呼吸点亮。
- **右键长按 + 拖动沿岛体边缘滑行**:左/右缘竖条、上/下缘横条(过角实时换向);指针钳制在岛体边缘带(**空气墙**,可绕边滑行);靠近插槽锚点(40px 内)**强引力**拉向插槽中心;顶墙微红反馈;松手回弹吸附最近插槽(8 个:4 角 + 4 边中点)并上报主进程(停靠飞行目标)。

### 渲染端视觉(60fps 本地 + 低频同步)

- **潮汐形变**:拖拽方用「指针 - 按下偏移」本地估计本窗口位置,与同步到的对方边界算距离(≤190px)→ 接触面"水舌"隆起(椭圆渐变,强度∝近距)+ 岛体轻微被拉向对方(±5px);目标侧经 sync.prox ~20Hz 低频驱动(CSS 过渡平滑)。
- **LOGO 球**:紧凑岛近距展开岛时 wrap 缩放(1→0.42)+ 文字淡出 + 圆角收圆,球元素(深色圆 + 强调色光环)随距离缩放,停靠飞行中收进切换条。
- **黑洞**:事件视界(暗色扭曲圆 + 脉冲)+ 7 颗错峰粒子被吸向视界 + 岛体 skew/blur 沿拉扯方向。
- 全部曲线:`cubic-bezier(0.34,1.56,0.64,1)` 回弹(进出场/吸附/分割线)/ `cubic-bezier(0.22,1,0.36,1)` 无过冲(插槽移动/滑行)/ wrap transform 0.12s linear(60fps 跟手)。

### 架构要点(踩坑实录)

- **主进程**:`dualWins.music/agent` + `dualState`(stack/dock/expanded/slots/bounds/anim/prox/drag);窗口 = 岛体 + 2×56 PAD(切换条活动区,岛体水平垂直双居中,窗口尺寸经 `dual:island-size` 上报后 `sizeDualWindow` **中心保持**调整);堆叠/球态窗口 = 岛体/球尺寸精确(无 PAD)。**sizeDualWindow 钳制下限 34**(LOGO 球直径)——原 80 会把 56 的堆叠窗口钳成 80(实测)。窗口补间 `tweenDual`(16ms 节拍,进度经 sync.anim 驱动渲染端动画)。近距广播 ~20Hz 节流。**switch_to_music 双岛禁用**:`engineConfig()` 每轮注入 excludedTools(工具列表同步显示禁用,不持久化)。Agent 事件/确认门路由到 agentWin;设置桥 executeJavaScript 固定 musicWin。
- **渲染端**:WidgetApp 拆调度器(role + mode 分支)→ SingleWidgetApp(原逻辑逐字保留)/ MusicDualApp / AgentDualApp(共用 DualStage 外壳:stage 右键拖拽 + 切换条 + 特效 + ResizeObserver 尺寸上报)。双岛窗口**禁用悬停伸缩/紧凑进度条**(岛体宽度稳定,配合堆叠/停靠几何;DynamicIsland 新增 `disableHoverExtend`/`disableHoverProgress`/`expandSeq` 三 prop);**禁用三连击/左滑右滑快捷切换**(音乐窗口不传 onAgentTripleClick,Agent 窗口不传 onAgentSwipeToMusic;模式切换走切换条)。Agent 设置从 Agent 岛 ⋯ 菜单进入,托盘"设置"固定开音乐窗口(背景/主题/字体/帮助)。窗口间定制同步:useIslandCustomizations 三个 hook 新增 **storage 事件跨窗口重读**(同一 origin localStorage 共享;背景/字体 IndexedDB 也同源共享)。
- **选择器陷阱**:根元素同时带 `widget-root` 与 `dual-window` 两个类,覆盖根样式必须用**复合选择器** `.dual-window.widget-root`——`.dual-window .widget-root`(后代组合器)不匹配自身(实测:padding 覆盖失效,Agent 拉伸差 20px 的背景填充露馅)。
- **巡检**:`WIDGET_SCREENSHOT_MODE=dual` 走 `electron/dual-shot.cjs`(主进程直接驱动组合状态机:独立并存 → 堆叠 → 展开+切换条 → 停靠飞行 → 停靠 → 解除 → 切换主体岛,11 张截图 + 每阶段 DOM 状态转储:窗口/岛体/wrap 类/切换条几何/root padding)。常规 WIDGET_SCREENSHOT 强制单窗口音乐模式(既有测试资产不受影响)。**验证流程**:先 `powershell Stop-Process -Name electron`(单实例锁 + 上轮 timeout 只杀 pnpm 不杀 electron 的残留进程),再带 WIDGET_SCREENSHOT_QUIT=1 运行。
